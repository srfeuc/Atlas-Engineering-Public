# PVE02 — Change Ledger (`Changes/`)

> **Post-build change records for the EQR6 hypervisor.** One file per change — `CM-####-Short-Kebab-Title.md`, continuing the estate CM sequence — recording **what changed, why, and the read-back evidence**. The rule: **never silently edit the `Build-Record`** — record the change here first, then update the record.

_No change records yet — PVE02 is not built. The first entries will come from the `221` bring-up (host stood up, always-on tier migrated). Current target state is in `../Build-Record.md` + the `221` runbook._
