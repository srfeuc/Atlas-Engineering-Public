# MKT01 — Change Ledger (`Changes/`)

> **Post-build change records for the E-W firewall / inter-VLAN gateway.** One file per change — `CM-####-Short-Kebab-Title.md`, continuing the estate CM sequence — recording **what changed, why, and the `print`-evidence**. The rule: **never silently edit the `Build-Record`** — record the change here first, then update the record. 🔴 **Every firewall-rule change is high-blast-radius** (MKT01 is in-path for all inter-VLAN) — log it, and note the console-break-glass state before applying.

_No change records yet — current state is in `../Build-Record.md` + `../Diagnostics.md`. Phase-7 rule additions are tracked in `../Incremental-East-West-Firewall-Build-Worksheet.md` until they land, then summarized here._
