# PFSENSE01 — Change Ledger (`Changes/`)

> **Post-build change records for the inline IPS.** One file per change — `CM-####-Short-Kebab-Title.md`, continuing the estate CM sequence — recording **what changed, why, and the evidence**. The rule: **never silently edit the `Build-Record`** — record the change here first, then update the record. 🔴 **Enabling a Suricata category inline is high-blast-radius under fail-closed** (an untuned rule cuts the internet) — log each category enable + the tuning that justified it, and note the break-glass state.

_No change records yet — the device is not built (design decided, hardware pending). Current state: `../Build-Record.md` (⬜) + `../Roadmap.md`._
