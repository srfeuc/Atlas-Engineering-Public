# FGT01 — Change Ledger (`Changes/`)

> **Post-build change records for the perimeter firewall.** One file per change — `CM-####-Short-Kebab-Title.md`, continuing the estate CM sequence — recording **what changed, why, and the `get`-evidence**. The rule: **never silently edit the `Build-Record`** — record the change here first, then update the record. 🔴 **UTM-profile + local-in-policy changes are high-blast-radius** (FGT01 is the single N-S chokepoint) — log them, note the break-glass state, and record the `get system status` subscription/DB check that justified trusting the profile (`ADR-0047`).

_No change records yet — current state is in `../Build-Record.md` + `../Diagnostics.md`._
