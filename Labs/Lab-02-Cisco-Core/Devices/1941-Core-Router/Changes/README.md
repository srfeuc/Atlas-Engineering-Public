# 1941 — Change Ledger (`Changes/`)

> **Post-build change records for the core router.** One file per change — `CM-####-Short-Kebab-Title.md`, continuing the estate CM sequence — recording **what changed, why, and the `show`-evidence**. The rule: **never silently edit the `Build-Record`** — record the change here first, then update the record.

_No change records yet — current state is in `../Build-Record.md` + `../Diagnostics.md`._
