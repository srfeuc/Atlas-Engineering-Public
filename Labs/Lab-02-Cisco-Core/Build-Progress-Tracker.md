---
Title: Lab-02 Build — Progress Tracker & Command Center (LIVING)
Path: Labs/Lab-02-Cisco-Core
Status: 🟢 LIVING — the **execution log** (live status + connectivity verification + troubleshooting). The plan/order owner is now `Operations/Build-Order-and-Dependencies.md` (`ADR-0043` / register E2). Update as each step lands.
Version: 0.22
Date: 2026-07-30
---

# Lab-02 Build — Progress Tracker & Command Center

> **Reconciliation:** `Operations/Build-Order-and-Dependencies.md` is the **plan** (order + gates + dependency map, `ADR-0043` / E2). **This is the execution log** — the actual order, live status, cross-device connectivity checks, and every problem hit + how it was fixed. When they diverge, note it here; the plan stays the reference (Rule 13).

## 🎯 Current focus

> **"Where we are" has one owner: `SESSION-HANDOFF.md` — its pinned 📍 CURRENT STATE block (`ADR-0049`).** Read that first. **This tracker owns the *execution log*** — commissioning order, connectivity matrix, device/guide status, done log, troubleshooting log. When the two overlap, the handoff holds the narrative; this holds the checklist. **Note (2026-07-30):** the **active thread now is the docs page-set replication wave** — its status lives in the handoff STATE block + `Operations/Device-Page-Set-Replication-Prompt.md`; **this tracker is the *physical-build* execution log** (networking → DC → AD CS → …), which resumes at the bench. *(B5/F8/F9: the old ~800-word "current focus" prose was consolidated into the handoff, 2026-07-28.)*

**Now (2026-07-28):**
- ✅ **Networking foundation + internet live**; ✅ **Hardening Pass-1** device-verified on all three network devices (MKT01/SW01/1941, 07-22).
- ✅ **DC01** promoted `atlas.lab` — OU skeleton, GPO 7a/7b/7c/7c-DSRM, **Stage-8 accounts/groups** all device-verified.
- 🟡 **DC02** operator-reported promoted (07-28) — `repadmin`/`dcdiag` **read-back pending** (see `DC-Domain-Controllers/Diagnostics-DC02.md`).
- ⏳ **AD CS in build** — ICA01 online `10.20.0.4`; the **RCA01 offline-root ceremony** is the gating next step.
- ✅ **Docs:** Phase-2 reconciliation + **C7/`ADR-0034`** (PVE01-networking ownership) complete; **B2–B5 learnability** + **per-device `Diagnostics.md` seeded** (8 devices) 2026-07-28.

**Next:** B1 repo front-door/glossary · finish the AD CS ceremony · **DC02 read-back** · D5 migrate-and-test lab (on-site).

> **Session snapshots (history — the handoff is the live owner):** 07-21 DC01 promo + GPO 7a; 07-22 GPO 7b/7c + Stage 8 + network-device Pass-1; 07-23 AD CS in execution (ICA01 online, Ubuntu golden image); 07-28 docs-reconciliation waves (`ADR-0029`..`0034`) + B-series/Diagnostics. Full narrative in `SESSION-HANDOFF.md`.

> **Build-order model (agreed 2026-07-21):** **Networking (all, permissive) → Hardening Pass 1 (self-contained, perimeter-first) → Services (identity/DC + NetBox + visibility) → Hardening Pass 2 (AD/PKI-backed auth) → Phase 7 east-west deny.** Box hardening ≠ east-west segmentation — the latter stays late and evidence-based. See `Operations/Build-Order-and-Dependencies.md` (the order owner).

## 📐 Commissioning order (phased, with gates)

**Phase 2 — Networking foundation (PERMISSIVE):**
| # | Step | Status |
|---|---|---|
| 1 | FGT01 `internal1` transit `10.255.255.1/30` | ✅ device-verified |
| 2 | 1941 base + SSH + two /30s + loopback + OSPF + default | ✅ verified + **saved** (`write mem`) |
| 3 | FGT01 ↔ 1941 /30 link (ping) | ✅ verified |
| 4 | MKT01 bridge-trunk + 9 VLAN gateways + `ether1` uplink + OSPF + default + `ether2` mgmt-fallback | ✅ **device-verified 07-21** (RouterOS auto-saves) |
| 5 | SW01 Lab-01→Lab-02 delta (VLAN 90, /27 SVI, SPAN src `Gi1/0/1`, trunk) + PVE01 trunk DAI trust | ✅ verified + **saved** 07-21 |
| 6 | MKT01 `ether1` ↔ 1941 `Gi0/0` cabled; OSPF **Full** | ✅ Full, stable 2h+ |
| 7 | First host on SW01 VLAN-10 reaches the interior | ✅ |
| 8 | FGT01 routing — interior return `10.0.0.0/8`→1941 + wan1 default (DHCP, already present) | ✅ 07-21 |
| 9 | FGT01 egress policy + NAT | ✅ 07-21 → **internet live** |
| 10 | FGT01 traffic logging (memory + `fwpolicy-implicit-log`) | ✅ 07-21 |
| 11 | **PVE01** VLAN-aware bridge/trunk to SW01 `Gi1/0/4` | ✅ verified — trunks VLAN 20 to the DC |

**Phase 2.5 — Hardening Pass 1 (self-contained, perimeter-first — no service deps):**
| # | Step | Status |
|---|---|---|
| 12 | FGT01 Guide 2: named admin ✅ + trusthost ✅ + **default `admin` removed ✅** → FortiToken MFA **deferred to Pass-2** (no mail/FortiCare; account already trusthost+strong-pw+break-glass) → remaining: strong crypto/TLS, disable unused services, encrypted backup, DoS policy | 🔄 |
| 13 | MKT01 service hardening (named admin/disable `admin`, disable unused services, scope ssh/winbox to mgmt, `mac-server` lockdown, SNMP off, NTP) | ✅ **device-verified 07-22** (Pass 1; guide v0.7) |
| 14 | SW01 / 1941 CIS passes (much already in from base build) | ✅ **BOTH device-verified 07-22** — SW01 (`CIS-Hardening-SW01` v1.0) + 1941 (`CIS-Hardening-1941` v1.0). Network-device Pass-1/CIS complete. |

**Phase 3+ — Services (identity-leaning; DC VM ready):**
| # | Step | Status |
|---|---|---|
| 15 | **DC01** — ✅ promoted `atlas.lab` + KDS + AD-DNS + PDCe time + **OU skeleton** + **GPO 7a (baseline+Wave-A)** + **7b PSO** + **7c LAPS** + **7c-DSRM** (all device-verified). **Stage 8 📋 authored** (groups/accounts/off-built-in-Admin/DC02) — awaiting execution. **Remaining:** run Stage 8 → **7d tier-deny** (unblocked) · GPO Wave B (VBS/CG) | 🔄 **now** |
| 16 | **NetBox** — source of truth (SW01 DAI/`STATIC-HOSTS` regenerated from here) | ⬜ (can run in tandem with 15) |
| 17 | Core services: Pi01 (DNS/NTP), SRV01 (Oxidized/syslog), MON01 (visibility/NetFlow) | ⬜ |

**Hardening Pass 2 (needs DC/CA) + Segmentation:**
| # | Step | Status |
|---|---|---|
| 18 | AD-backed LDAPS admin auth on FGT01 + devices (`fortigateadmin` stays the local break-glass); optional PKI cert-auth as an advanced elective | ⬜ gated on DC complete (+ CA for PKI) |
| 19 | MKT01 east-west **default-deny** (from NetFlow evidence) | ⛔ gated on console cable + MON01/NetFlow |
| 20 | FGT01 security profiles (Guide 3 — web/DNS/AV/IPS) | ⛔ FortiGuard/UTP |

> **Divergence note (Rule 13):** `Master-Build-Order.md` orders NetBox (Phase 3) before the DC (Phase 5). Execution is leaning **DC early / in tandem** — it's the identity+DNS+NTP backbone and unblocks AD-backed hardening. Plan stays the reference; this is the intentional deviation.

## 📊 Connectivity verification matrix
Key IPs: FGT01 transit `10.255.255.1`, 1941 `10.255.255.2`(FGT)/`10.255.255.5`(MKT)/Lo `10.255.0.1`, MKT01 `10.255.255.6`/Lo `10.255.0.2`/VLAN-gw `10.<vlan>.0.1`, SW01 SVI `10.10.0.2`, host `10.10.0.20`, DC01 `10.20.0.2`.

| From → To | Expect | Status |
|---|---|---|
| 1941 → FGT01 `.1` | 100% | ✅ 07-20 |
| FGT01 → 1941 `.2` / `.5` / Lo `10.255.0.1` | 100% | ✅ 07-21 |
| FGT01 → MKT01 VLAN gw `10.10.0.1` | 100% | ✅ 07-21 |
| MKT01 ↔ 1941 OSPF | **Full** | ✅ 07-21 (stable 2h+) |
| Host(VLAN10) → MKT01 `10.10.0.1` / Lo `10.255.0.2` / other VLAN gws | 100% | ✅ 07-20 |
| Host(VLAN10) → 1941 `10.255.255.2` | 100% | ✅ 07-21 |
| **Host(VLAN10) → internet `1.1.1.1`** | 100% | ✅ **07-21 (internet live)** |
| FGT01 → internet `1.1.1.1` | 100% | ✅ 07-21 |
| DC01(VLAN20 `10.20.0.2`) → gw `10.20.0.1` + internet | 100% | ✅ 07-21 (after PVE01-trunk DAI trust) |

**OSPF:** 1941 ↔ MKT01 **Full** on `10.255.255.4/30` (RIDs 1941=`10.255.0.1`, MKT01=`10.255.0.2`); 1941 ↔ FGT01 **no neighbor** (FGT01 = static, 1941 `Gi0/1` passive — correct). MKT01 learns the 1941 loopback + FGT-side transit as OSPF; the 1941 learns MKT01's VLANs as O E2 (`redistribute=connected`). *(Note: the /30 runs OSPF "broadcast" type — DR/BDR elected; harmless, could be set point-to-point.)*

## 📋 Device / guide status
| Device | Guide / step | Status | Blocked on |
|---|---|---|---|
| FGT01 | Guide 1 — Networking (Steps 1–4) | ✅ **device-verified 07-21 — internet live** (v0.5) | — |
| FGT01 | Logging & Flow-Tracing Field Guide | ✅ authored (v0.1) — operator reference | — |
| FGT01 | Guide 2 — Hardening (Pass 1) | 🔄 named admin + trusthost + default `admin` removed; MFA → Pass-2; crypto/services/backup remain | — |
| FGT01 | Guide 3 — Security Profiles | ⛔ | FortiGuard/UTP |
| 1941 | Build-Guide — Stages 1–3 | ✅ device-verified + saved (v1.2) | — |
| 1941 | **CIS Pass-1** (`CIS-Hardening-1941` v1.0) | ✅ **CIS device-verified 07-22** (SSH crypto CTR/DH2048; MACs SHA1-only + no KEX-pin = older-ISR IOS limit, DH2048 mitigation; client `~/.ssh/config` legacy-algo flags; vty `access-class` both ranges/scrypt/NTP→DC01) | Pass-2 (RADIUS/TACACS admin) = later; ⏳ NTP re-check `synchronized` |
| MKT01 | Build-Guide — bridge/VLAN/uplink/OSPF/default/mgmt | ✅ **device-verified 07-21** (v0.4) | — |
| MKT01 | Service hardening (Stage 1 / Pass 1) | ✅ **device-verified 07-22** (v0.7 — admin/services/scope/mac/SNMP/NTP) | Pass 2 (AD-LDAPS) = Phase-3-dependent |
| MKT01 | Incremental FW worksheet — Phase 7 | ⛔ deferred | console cable + MON01 |
| SW01 | Build-Guide (v0.6) + **CIS Pass-1** (`CIS-Hardening-SW01` v1.0) | ✅ networking verified; ✅ **CIS device-verified 07-22** (SSH crypto/vty/scrypt/NTP/VLAN1) | Pass-2 (RADIUS/TACACS admin) = later |
| PVE01 | VLAN-aware bridge / trunk | ✅ verified — trunks VLAN 20 to the DC | — |
| DC01 | Build-Guide (GUI-primary, v0.4) — promote/KDS/DNS/time | ✅ **`atlas.lab` promoted + KDS + PDCe time 07-21** (Stages 0–5) | — |
| DC01 | `OU-Design-and-Build.md` (v0.2) — OU skeleton | ✅ **built & device-verified 07-21** (`Devices`/`Employees` rename; `redircmp`→Staging) | — |
| DC01 | `GPO-Design-and-Build.md` (v0.7) — Stage 7 (GPOs) | 🔄 **7a + 7b + 7c + 7c-DSRM done** (device-verified); Wave B + 7d remain | Wave B: Proxmox VBS check · 7d: **now unblocked** (tier groups authored in Stage 8) |
| DC01 | `Tiered-Admin-and-Groups-Build.md` (v0.4, **GUI-first**) — Stage 8 (groups/accounts/off-built-in-Admin) | 🔄 **Part 2 groups ✅ device-verified 07-22**; Part 3 accounts + off-built-in-Admin next | Seth to run Part 3 in ADUC/ADAC |
| DC02 | `DC02-Build-Guide.md` (v0.1) — replica DC (`10.20.0.3`) | 🟡 **operator-reported promoted 2026-07-28** — `repadmin`/`dcdiag` read-back PENDING (POL-0001) | Confirm at lab: `Get-ADDomainController DC02`, `repadmin /replsummary` = 0 failures |
| ~~CA01~~ | **RETIRED — not built** (`ADR-0031`): OpenSSL Lab CA retired; CA01-VAULT01 host decommissioned; Vaultwarden survives standalone | ⛔ **struck** | Non-domain devices (FGT/MKT/Pi-hole) fold onto **AD CS (ICA01)** via the D5 migrate-and-test lab. |
| RCA01 + ICA01 | **AD CS two-tier PKI** — `RCA01-ICA01-ADCS/AD-CS-Two-Tier-Build-Guide.md` (v0.2, MS-audited) | 🔄 **in execution (07-23)** (`ADR-0027`) | **ICA01 ✅ online `10.20.0.4`** (golden-image clone, patched; domain-join → CA install next). **RCA01** = full-GUI golden-image clone (🔴 not Server Core — offline root can't be remote-managed, so GUI-primary = Desktop Experience), **patching** → offline ceremony. Deliverable: **DC LDAPS cert** (unblocks device-auth wave). |
| — | Per-device `Troubleshooting.md` (SW01/MKT01/1941/FGT01/DC01) | ✅ authored 07-21 | — |
| — | VM Snapshot & Naming Convention standard (`218-…`) | ✅ authored 07-21 | — |

## 🔴 Verify-on-resume (save-state & open confirmations)
1. ✅ **SW01 saved** — `copy running-config startup-config` ran with the DAI-trust fix (07-21).
2. ✅ **1941 saved** (`write mem`; OSPF Full 2h+ proves live config).
3. ✅ **MKT01 device-verified 07-21** (all 9 gateways, uplink, loopback, OSPF Full, default via `.5`, `ether2` mgmt-fallback present/inactive-until-plugged). RouterOS auto-saves.
4. ✅ **FGT01** — internet live; break-glass `192.168.1.99` intact (`ping https ssh fabric`); `fortigateadmin` + trusthost working; default `admin` **deleted** (fortigateadmin is now the sole admin — record its password; consider a 2nd local break-glass admin); MFA deferred to Pass-2. 🔴 Interim: **HTTPS enabled on `internal1`** (transit) for interior GUI — revisit in Guide 2.
5. ✅ **PVE01** — bridge trunks VLAN 20 (proven by the DC reaching its gateway once `Gi1/0/4` was DAI-trusted).
6. ✅ **DC01 promoted `atlas.lab`** (07-21, Stages 0–5). ✅ **DSRM password now LAPS-managed** (07-22, §7c-DSRM) — `POL-0002` manual-record **retired**; retrieve on demand with `Get-LapsADPassword -Identity DC01 -AsPlainText`. KDS root key lives in AD (system-state backup). Snapshot `dc-atlaslab-<date>` recommended; consider a 2nd local admin.
7. ✅ **DC01 OU skeleton built** (07-21) — `Get-ADOrganizationalUnit` matches `OU-Design-and-Build.md`; DCs still in built-in `Domain Controllers` OU; `Get-ADDomain` ComputersContainer = `OU=Staging,OU=Devices`.
8. ✅ **DC01 GPO 7a Wave A applied & verified** (`gpresult /r`, 07-21). ✅ **7b PSO** (`PSO-FinanceHR`→`G-FinanceHR-Users`, lockout 3), **7c LAPS** (schema+self-perm+`LAPS`→Devices; member test deferred), **7c-DSRM** (fully verified) — all 07-22. 🔴 **Wave B (VBS/CG) NOT yet linked** — gated on a Proxmox `msinfo32` VBS check (CPU `host` + UEFI/OVMF + vTPM). 🔴 **7d tier-deny** needs the Stage-8 tier admin groups to exist first.

## 🔒 Standing blockers / gates
- **Validation / adversarial-test (pen-test) pass** (backlog, Seth 07-22) → `Operations/Validation-and-Adversarial-Testing.md` (stub created). Control→attack→evidence matrix that *proves* each control (tier-deny logon, LAPS read boundary, PSO applies, baseline enforced, east-west deny); later add PingCastle / Purple Knight / BloodHound. Most rows gate on Stage-8 tier accounts/groups + segmentation.
- **7d tier-deny logon GPOs** → build steps depend on the **Stage-8 tier admin groups** (`G-Tier0/1/2-Admins`). Those are now **📋 authored** (`Tiered-Admin-and-Groups-Build.md` §2a) — once Seth *executes* Stage 8, 7d is fully unblocked (names are already concrete in `GPO-Design-and-Build.md` §7d).
- ~~DSRM password recorded on paper (`POL-0002`)~~ → **RETIRED 07-22** — LAPS-managed & rotating (§7c-DSRM); retrieve via `Get-LapsADPassword`.
- ~~wan1 not cabled~~ → **RESOLVED** — wan1 up on DHCP (`172.31.4.1`), default route auto-installed; internet live.
- ~~MKT01 ↔ 1941 cable~~ → **RESOLVED** — cabled, OSPF Full.
- ~~DC not built~~ → **core identity up** (`atlas.lab` promoted). Pass-2 AD-backed admin auth waits on the DC being **complete** (OU/GPO/tiering).
- 🔶 **Phase 2.5 box hardening — network devices done.** ✅ **MKT01 Pass-1** + ✅ **SW01 CIS** + ✅ **1941 CIS** all COMPLETE 07-22 (all three network-device Pass-1/CIS passes device-verified); FGT01 Pass-1 *core* done (crypto/backup/DoS remain). Only ⬜ **FGT01 Pass-2 AD-LDAPS** (unblocked) remains for Phase 2.5. All follow `Operations/Device-Hardening-Standard.md`. ⏳ **1941 NTP** — re-check `show ntp status` flips to `synchronized, stratum 3` (was converging at capture).
- 🟢 **FGT01 Pass-2 (AD-backed admin) — auth path RESOLVED (07-23): `ADR-0028` = direct LDAPS.** "The FortiGate needs an AD account to secure it." Chose **Option B (direct FGT→LDAPS to the DC)** over RADIUS — a documented, FGT-specific deviation from `ADR-0004` (learning goal + FortiOS-native LDAPS); `fortigateadmin` stays local break-glass. Still gated on the **DC LDAPS cert** (AD CS Part 3.4). **Part-1 AD prereqs buildable now:** `G-Network-Admins` + least-priv `svc-fgt-ldap` (`Build-Guide-2b-AD-LDAPS-Admin.md`).
- 🟡 **AD CS two-tier PKI (`ADR-0027`) — CURRENT WORKSTREAM.** Build the Windows-domain PKI the Microsoft way: **RCA01** offline standalone root → **ICA01** enterprise issuing CA (the renamed golden-image clone). Hard gate: **working HTTP CDP/AIA revocation** (`pkiview` all-OK + an observed revocation — the `ADR-0009` correction). Produces the **DC LDAPS cert** the whole device-auth wave waits on. Guide authored (`RCA01-ICA01-ADCS/`), awaiting execution. 🔴 **Updated 2026-07-28:** now the estate's **only** PKI — the OpenSSL CA is **retired** (`ADR-0031`); non-domain devices (FGT/MKT/Pi-hole) fold onto AD CS.
- ~~Open Stage-8 closeouts (seth-move, §3e, G-IT-Staff-move)~~ → ✅ **DONE 07-22** (seth→`Employees\IT`; Administrator rotated+parked, pw in Vaultwarden; `G-IT-Staff`→`Security-Roles`). Remaining Stage-8: **DC02 promotion** + tiered LAPS read test (needs a member server).
- 🔴 **Git** — Seth still needs to `git add -A && git commit && git push` the accumulated 07-22 doc changes (bot can't commit).
- **Console break-glass (FTDI, `ADR-0016`)** — intended durable/OOB mgmt for MKT01 **and** the gate for the Phase-7 east-west deny. ✅ **Cable ACQUIRED 07-22** (was spec'd-not-acquired) → the Phase-7 east-west deny is no longer hardware-blocked; still needs MON01/NetFlow evidence before enabling. (`ether2` `192.168.88.1` remains the interim mgmt path.)
- **CA (Phase 8 — AD CS)** → PKI cert-auth (optional). **AD CS (ICA01/RCA01) is the only PKI**; OpenSSL CA01 retired/not built (`ADR-0031`).
- **NetBox** → SW01 DAI (NetBox-generated ACL).
- **MON01 + NetFlow** → NTP/SNMPv3/syslog on all; the evidence for Phase-7 deny.
- **FortiGuard/UTP** → FGT01 security profiles (Guide 3).

## 🐞 Troubleshooting log (issues hit + fix) — newest first
| Date | Device | Problem | Root cause | Fix |
|---|---|---|---|---|
| 07-22 | PVE01/SW01 | **Win11 VM on VLAN 10 can't reach its gateway** (image build) | VLAN 10 is the **native** VLAN on the PVE trunk (`Gi1/0/4`); a **tagged-VLAN-10 VM** gets return traffic sent *untagged* by the switch → VLAN-aware bridge won't deliver it to a tag-10 vNIC (asymmetry). NOT DAI (DAI is off on VLAN 10). | Put VM workloads on a **tagged** VLAN — moved the image to **VLAN 20** (10.20.0.40/26) and it worked. Design rule: VLAN 10 (native) = infra mgmt only; VMs on tagged VLANs. Captured in `Master-Build-Order` Phase 2 + SW01 failure modes. |
| 07-22 | PAW/desktop | `ssh ciscoadmin@sw01` → "REMOTE HOST IDENTIFICATION HAS CHANGED / Host key verification failed" | SW01's SSH RSA key was regenerated during the build → client's stored `known_hosts` fingerprint no longer matches (strict checking) | `ssh-keygen -R 10.10.0.2` + `ssh-keygen -R sw01`, reconnect, accept the new key. (Documented in `SW01/Troubleshooting.md`.) |
| 07-22 | DC02 | **Add Roles & Features fails** — "WinRM service is not running, attempts to start failed" | Freshly-cloned VM: NIC in **Public** network profile (WinRM refuses to configure) + WinRM not started | `Set-NetConnectionProfile -NetworkCategory Private`; `Set-Service WinRM -StartupType Automatic`; `winrm quickconfig`. Or bypass the GUI: `Install-WindowsFeature AD-Domain-Services -IncludeManagementTools` (local, no WinRM). Likely baked into the golden image — clean up the template later. |
| 07-22 | DC01 (t0-seth) | **RDP as `t0-seth` → "A user account restriction … is preventing you from logging on"** | `t0-seth` is in **Protected Users** (no NTLM); RDP **by IP** forces NTLM → `STATUS_ACCOUNT_RESTRICTION`. Working as designed. | RDP **by name/FQDN** (Kerberos) from a domain-joined box, or administer from the **PAW/console**. Do **not** remove from Protected Users. This motivated building PAW01. |
| 07-22 | DC01 | **Subnet mask wrong** — `255.255.224.0` (/19) live vs plan's **/26** | NIC mask mis-entered at build | `Set-NetIPAddress -InterfaceAlias Ethernet -IPAddress 10.20.0.2 -PrefixLength 26` → confirmed /26. (Reminder: `Set-NetIPAddress` can drop the default gateway — re-check `Get-NetIPConfiguration`.) |
| 07-21 | DC01 | Found already promoted to the **wrong domain** (`atlas.local`) | An earlier promotion used `atlas.local` instead of `atlas.lab` (`ADR-0007`); `.local` collides with mDNS. Domain name is fixed at promotion | **Demote** (`Uninstall-ADDSDomainController -LastDomainControllerInDomain`) → patch → **re-promote `atlas.lab`** (`Install-ADDSForest`). Wrong domain = rebuild, not rename. Verified `Get-ADDomain` → `atlas.lab`. |
| 07-21 | DC01 | DC time source = `Local CMOS Clock` | PDC-emulator not pointed at an external source (VM/CMOS time) | `w32tm /config /manualpeerlist:"time.nist.gov,0x8 …" /syncfromflags:manual /reliable:yes /update` → `/query /source` = external. If it reverts, disable QEMU guest time-sync. |
| 07-21 | SW01 / DC01 | DC on VLAN 20 couldn't reach its gateway (no internet, no RDP) | DAI is **on for VLANs 20–90**; the DC (static, no DHCP-snooping binding) has its ARP dropped on the **untrusted PVE01 trunk** `Gi1/0/4` | `interface Gi1/0/4 → ip arp inspection trust` (trust the hypervisor uplink, like the MKT01 trunk). Confirm via `show ip arp inspection statistics vlan 20`. Folded into SW01 guide v0.5. |
| 07-21 | SW01 | SSH from Windows fails: `no matching key exchange method found` (then host-key, then MAC) | Old IOS offers only SHA1 KEX/host-key/MAC; modern OpenSSH disabled those (not a key-size issue) | Client: `ssh -o KexAlgorithms=+diffie-hellman-group14-sha1 -o HostKeyAlgorithms=+ssh-rsa -o MACs=+hmac-sha1 …`; after regenerating the key clear the stale one: `ssh-keygen -R 10.10.0.2`. |
| 07-21 | FGT01 | Host reached the 1941 but not the internet, while the FortiGate itself could ping out | No **egress firewall policy** — interior→wan1 traffic hit the implicit deny (self-originated FGT traffic isn't subject to the forward policy) | Add the `egress-interior-to-wan` policy (`internal1`→`wan1`, NAT on, log all). Internet immediately live. |
| 07-20 | SW01 | Static VLAN-10 host can't reach the gateway | `ip arp inspection filter STATIC-HOSTS vlan 10` drops ARP for unlisted hosts | Remove VLAN 10 from DAI (left OFF until NetBox regen); add host MAC as interim. |
| 07-20 | SW01 | `homelab` v2c SNMP community | Carried from Lab-01 | Remove; SNMPv3 → MON01 in Phase 6. *(Confirmed already absent in the 07-21 live config.)* |
| 07-20 | SW01 | SPAN captures nothing | Session had a destination but no source | `monitor session 1 source interface Gi1/0/1`. |
| 07-20 | SW01 | `switchport trunk encapsulation dot1q` → invalid | 2960X is 802.1Q-only | Omit the line. |
| 07-20 | MKT01 | Locked out after power cut | Only the transit /30 was IP'd; defconf gone | Recover via MAC-WinBox; add durable mgmt IP on `ether2`. RouterOS auto-saves. |
| 07-20 | MKT01 | `passive=yes` on OSPF template → error | Not a valid property in 7.23.1 | `redistribute=connected` on the instance (OSPF on the transit only). |
| 07-20 | MKT01 | `etherA/etherB` / bridge-vlan errors | Placeholders + wrong VLAN model for the RB1100AHx4 | `ether1`=uplink, `ether3`=trunk; VLAN sub-interfaces on `bridge-trunk`, `ether3 hw=no` (RTL8367 offload trap). |
| 07-20 | 1941 | `crypto key`/`service timestamps` errors; SSH login refused | Exec-vs-config mode; `login` not `login local` | `conf t` first; crypto key on its own line; `login local` on vty (Stage 1b). |
| 07-20 | 1941 | OSPF wouldn't form / no VLAN routes | `network` listed VLAN subnets, omitted the /30s | OSPF on the two /30s + loopback only; `passive Gi0/1`; VLANs learned. |
| 07-20 | FGT01 | `edit port2` → "vdom MUST be set" | 60E has no `port2` (interior = `internal` hardware switch); single-VDOM | Split `internal1` off the switch; no `set vdom`. |

> Detailed per-device write-ups now live in each device's `Troubleshooting.md`; this table is the cross-device timeline.

## 🧾 Done log (newest first)
- **07-22 (PM)** — 📄 **AD CS PKI direction set + designed (`ADR-0027` + build guide).** Operator chose to build the domain PKI **the Microsoft way, now** (PKI-first), to unblock the FGT/device-auth wave properly. Reconciled hard against the existing record (POL-0008): corrected an earlier "no CA yet" (a live **OpenSSL two-tier CA on Pi01** already issues the FGT cert, `ADR-0003`/`ADR-0009`), and confirmed network-device auth is **RADIUS** while **LDAPS is for services** (`ADR-0004`/`ADR-0021`). **`ADR-0027`** (v1.1) pins: two-tier AD CS (offline **RCA01** root + enterprise **ICA01** issuing = the renamed golden-image clone; **NOT** CA01, the OpenSSL intermediate), **working HTTP CDP/AIA revocation** (the `ADR-0009` fix, gated on `pkiview` + an observed revocation), RSA-4096/SHA-256, both CAs **Tier 0**, scope held on the `ADR-0003` domain/non-domain boundary. New living **`RCA01-ICA01-ADCS/AD-CS-Two-Tier-Build-Guide.md`** (v0.1, GUI-primary): rename-before-role gotcha, offline-root ceremony, enterprise-sub request→offline-sign→install, ESC hardening + Kerberos-Auth autoenrollment + **LDAPS(636) verify**, backup with the `ADR-0009` convergence/destroy discipline. Still open: the FGT auth path (RADIUS vs direct-LDAPS), settled after ICA01 is up. Grounded in MS PKI-design-considerations + the official two-tier Test Lab Guide (per `304`).
- **07-22 (PM)** — 🎯 **Phase-2.5 1941 CIS pass COMPLETE (device-verified) — all three network-device passes now done.** New `CIS-Hardening-1941.md` v1.0. SSH crypto tightened (CTR-only ciphers, DH 2048, retries 2 — CBC/3DES/1024 gone). Two honest older-ISR IOS **limitations** documented, not faked: **MACs SHA1-only** (no SHA2 on this image) and **`ip ssh server algorithm kex` doesn't exist on the 1941** (the router keeps *offering* SHA1 KEX groups) — mitigation is **`ip ssh dh min size 2048`** (floors the DH modulus so weak `group1`/768-bit won't negotiate). Because the box can't speak modern SSH, the required **client-side `~/.ssh/config` legacy-algo block** (`KexAlgorithms +diffie-hellman-group14-sha1`, `HostKeyAlgorithms +ssh-rsa`, `MACs +hmac-sha1`) is documented in the doc — this is the box's ceiling. **vty `access-class MGMT-SSH` on BOTH ranges** (`0 4` + `5 15`) + `exec-timeout`, secrets **Type 5 → Type 9 scrypt**, `login block-for` kept, **NTP → DC01** (`10.20.0.2`; `show ntp associations` → `*~10.20.0.2` selected sys.peer; ⏳ still converging at capture — re-check `synchronized`), host key regenerated once then left stable, config saved, console break-glass used/proven (SSH was blocked mid-hardening by client legacy-crypto until the `~/.ssh/config` flags were added). Deferred: MON01 logging/SNMPv3 (Phase 6), Pass-2 AAA (RADIUS/TACACS→SRV01/NPS). **Phase 2.5 network devices complete — only FGT01 Pass-2 AD-LDAPS remains.**
- **07-22 (PM)** — 🎯 **Phase-2.5 SW01 CIS pass COMPLETE (device-verified).** New `CIS-Hardening-SW01.md` v1.0. SSH crypto tightened (CTR-only ciphers, DH 2048, retries 2 — CBC/3DES/1024 gone; MACs stuck at SHA1 = 2960X IOS limitation, documented), **vty `access-class MGMT-SSH` + `exec-timeout`**, secrets upgraded **Type 5 (MD5) → Type 9 (scrypt)** + the generic `cisco` priv-15 account removed (single named admin `ciscoadmin`), **NTP synced to DC01** (stratum 3, `CM-0030` closed) with a stale `10.10.0.5` source removed, VLAN1 admin-down, host key left un-regenerated to stop `known_hosts` churn, config saved. No telnet/http (base build). Deferred: MON01 logging/SNMPv3 (Phase 6), DAI (Phase 4), Pass-2 AD auth.
- **07-22 (PM)** — 🎯 **Phase-2.5 MKT01 Pass-1 hardening COMPLETE (device-verified).** Console break-glass proven first (backup + serial login), then: SNMP off (only `public` — the `homelab`/CM-0023 finding was stale for MKT01, POL-0001), named admin `mikrotikadmin` + `admin` disabled, ssh/winbox scoped to `10.10.0.0/27,192.168.88.0/24` + strong-crypto, **`reverse-proxy` (443) disabled** (missed by the original list), mac-server/mac-winbox/mac-ping off, unused `ether4–13` disabled, cloud `update-time=no` (`ddns-enabled` has no `no` in 7.23.1 — `auto`=off), and **NTP fixed** (clock was stuck Jun/03, NTP off → synced to DC01 `10.20.0.2` stratum 2, tz Central). Lockout mid-way (Neighbors empty after discovery+mac-server off) → recovered by **WinBox-by-IP**; console was the safety net. MKT01 guide → v0.7 (device-verified marks + the reverse-proxy/cloud/NTP/lockout lessons). **First Phase-2.5 box done.**
- **07-22 (PM)** — 📄 **`Operations/Device-Hardening-Standard.md` created** — the shared recovery-first + Pass-1 checklist every device executes (per-device break-glass table; FGT01-modeled). MKT01 guide links to it. Applies to SW01/1941/FGT01 + future service devices.
- **07-22 (PM)** — 🎯 **Stage 8 Part 3 fully closed out (device-verified).** `seth` moved to `Employees\IT` (out of the mis-placed `Tier 2\Admin`); `G-IT-Staff` moved to `Groups\Security-Roles`; **built-in Administrator rotated** (`PasswordLastSet` 07-22, still Enabled, `PasswordNeverExpires`, password stored in Vaultwarden as break-glass). Tiered-Admin doc → v0.6. **Stage-8 accounts/groups complete** — remaining Stage-8 is DC02 promotion + the tiered LAPS read test (needs a member server). Next: **Phase 2.5** (MKT01/SW01/1941 hardening + FGT01 Pass-2 AD-LDAPS).
- **07-22 (PM)** — 📄 **Master-Build-Order → v1.3 (verbose + reality pass).** Expanded Phase 3 into sub-stages 3a–3g (incl. **PAW01** + the Windows golden-image prereq), fleshed out Phase 2.5 (MKT01/SW01/1941 CIS outstanding + a concrete **FGT01 Pass-2 AD-LDAPS** procedure now unblocked), added the native-VLAN VM-placement lesson (Phase 2) and PAW east-west flow (Phase 7), and added **Phase 11 — Hybrid Identity & Cloud (Azure/Entra)** with cost/sequencing. Added an honest "execution reality" note (Phase-2.5-partial, Phase-3-deep).
- **07-22 (PM)** — 📄 **PAW01 build guide authored (v0.4)** — `Devices/PAW01-Tier0-Admin/Build-Guide.md`. Microsoft-grounded (sysprep golden image, PAW/Enterprise Access Model, Win11 SCT baseline, RSAT); Part 1 golden image, Part 2 the PAW (clone→pre-stage in `Admin\Tier 0\PAW`→join→baseline+harden→RSAT), Part 3 the honest cloud-managed delta + **3.1 Azure cost/sequencing** (on-prem $0; EMS E3 $12 / E5 $18 ≤$20/mo; free dev tenant). Building it started: Win11 golden image in **Audit Mode** (noVNC `Ctrl+Shift+F3` gotcha; guest tools + updates in progress).
- **07-22 (PM)** — 🎯 **Stage 8 Part 3 accounts device-verified (mostly).** `t0-seth` (Domain Admins + `G-Tier0-Admins` + Protected Users), `t1-seth` (`G-Tier1-Admins` + Protected Users, **not** a DA), `seth` (`G-IT-Staff`) — all enabled, membership + Protected Users confirmed; **logon test as `t0-seth` passed** (before Protected Users added — correct order). Enterprise/Schema Admins clean (only built-in Administrator). ⏳ **Open:** `seth` is in `Tier 2\Admin` — **move to `Employees\IT`** (IT OU created ✅); **§3e** secure the built-in Administrator (still enabled, `PasswordLastSet` still 07-21 — rotate + park as break-glass). `G-IT-Staff` still in `Groups` root (Move pending).
- **07-22** — 🎯 **Stage 8 Part 2 (AGDLP groups) device-verified.** Seth built `G-Tier0-Admins`/`G-Tier1-Admins`/`G-Tier2-Admins` (+ `G-IT-Staff`) — `Get-ADGroup` confirms all **Global/Security**, tier groups in the correct `Admin\Tier X\Groups` OUs → the **7d deny-logon GPOs now have real groups to reference**. Caught a placement nit: `G-IT-Staff` landed in the `Groups` root instead of `Groups\Security-Roles` (recommended a GUI Move to match `G-FinanceHR-Users`). Tiered-Admin doc → v0.4 (§2 marked ✅). Part 3 (accounts + off-built-in-Administrator) next.
- **07-22** — 📄 **Tiered-Admin doc reworked GUI-first (v0.3).** Per Seth's request (distrusts PowerShell troubleshooting; wants the ADUC/ADAC way a junior would actually use), every Stage-8 build step is now a full click-path — group/account wizards, membership via each user's Member Of tab, RID-500 identification + Reset Password, Protected Users via the Members tab, the LAPS read grant via the Delegation of Control Wizard — with PowerShell demoted to optional collapsible blocks. IT-OU decision locked to `OU=IT,OU=Employees` (now a build step). Handed Seth the ordered runbook in chat.
- **07-22** — 📄 **DC02 split into its own living guide.** New **`DC02-Build-Guide.md`** (v0.1) — dedicated replica-DC build mirroring `DC01-Build-Guide.md`: the five replica traps (replica-not-forest, DNS island, `DOMHIER` time, FSMO-stays/transfer-not-seize, auto-inherited DC-OU policy), VM prep, `Install-ADDSDomainController`, DNS steady-state, the full `repadmin`/`dcdiag`/DFSR/object-flow verify, FSMO map + LAPS/baseline inheritance. The Stage-8 doc's Part 4 collapsed to a pointer (POL-0008); `DC01-Build-Guide.md` → v0.5 (Stage 8 pointer). 🔴 Authored, not executed.
- **07-22** — 📄 **Stage 8 build guide authored (not executed).** New living doc **`Tiered-Admin-and-Groups-Build.md`** (v0.1): Part 1 AGDLP + tier-model model with the `G-*`/`DL-*` naming convention; Part 2 builds the **tier admin groups `G-Tier0/1/2-Admins`** (the 7d prerequisite) + `G-IT-Staff`; Part 3 the **tiered accounts** `t0-seth`/`t1-seth`/`seth` — Domain-Admins-vs-tier-group distinction, logon-test-before-switch, **getting off the built-in Administrator** (secure as break-glass, don't disable), Protected Users (with the NTLM caveat), and the now-unblocked tiered LAPS read grant; Part 4 **DC02** promotion + replication verify. GUI-primary + PowerShell, 📸 marks. Made `GPO-Design-and-Build.md` §7d group names concrete (v0.7); Build-Checklist → v1.3. 🔴 **Awaiting Seth to execute on the devices + commit (POL-0001).**
- **07-22** — 🎯 **GPO Stage 7 continued: 7b + 7c + 7c-DSRM.** **7b** Finance/HR **PSO** built in ADAC (`PSO-FinanceHR`, prec 10, min 15, lockout 3) applied to new global group `G-FinanceHR-Users` — with the honest "measure the *live* domain default first" step (domain was already min14/42-day/lockout3, so 365+lockout5 would've been *looser*). **7c** Windows **LAPS**: schema extended (`Update-LapsADSchema`), self-perm on Devices, `LAPS` GPO → Devices (member live-test deferred until a server joins). **7c-DSRM** fully device-verified — `LAPS-DC-DSRM` GPO rotates & escrows the **DSRM password** on DC01 (`Get-LapsADPassword` → EncryptedDSRMPassword, Success), **retiring `POL-0002`**. Captured the admin-account-vs-service-account clarification (bootstrap as built-in Administrator → tiered `t0-seth` at Stage 8). `GPO-Design-and-Build.md` → v0.6. New backlog: a **Validation/adversarial-test pass**.
- **07-21** — 🎯 **DC OU skeleton + GPO baseline (Stage 6 → 7a).** OU tree built & device-verified (`Get-ADOrganizationalUnit`) — hit the `8305` reserved-name wall (root `Computers`/`Users` collide with the built-in containers) → renamed **`Devices`**/**`Employees`**, `_Admin`→`Admin`, per-tier Service Accounts + PAW OUs added; `redircmp`→`OU=Staging,OU=Devices`. **GPO Stage 7a:** imported the **MS Windows Server 2025 v2602 Security Baseline** (8 GPOs) via `Baseline-ADImport.ps1` run from its `Scripts\` folder (gotcha: don't run its guts interactively), then **linked & device-verified Wave A** — Domain Security→root (order 1), Domain Controller + Defender→`Domain Controllers` OU, Member Server→`Devices\Servers`, Defender + IE11→`Devices`; caught & fixed a mis-link (DC baseline had landed at the domain root → moved to the DC OU). `gpresult /r` on DC01 = all applied, no lockout post-reboot. New living docs: **`OU-Design-and-Build.md`** (v0.2) + **`GPO-Design-and-Build.md`** (v0.4, incl. the Part-1 GPO mental model). Wave B (VBS/CG) gated on a Proxmox VBS check.
- **07-21** — 🎯 **DC01 rebuilt correctly to `atlas.lab`.** Found promoted to the wrong domain (`atlas.local`) → demoted → re-promoted **`atlas.lab`** (`Get-ADDomain` confirmed) → **KDS root key** created (backdated) → **PDCe time** set to `time.nist.gov`. `DC01-Build-Guide` reworked **GUI-primary** (v0.2), Stages 0/2/3/5 device-verified. CA01 VM cloned (not started). New standard: **VM Snapshot & Naming Convention** (`218-…`). **Per-device `Troubleshooting.md`** created for SW01/MKT01/1941/FGT01/DC01.
- **07-21** — 🎯 **INTERNET LIVE, end to end.** Host `10.10.0.20` (VLAN 10) → MKT01 → 1941 → FGT01 → out. FGT01 Guide 1 complete + device-verified (v0.5): interior return `10/8`→1941, wan1 DHCP default (already present), egress policy + NAT, memory + implicit-deny logging. Root cause of the "host can't browse" gap was the missing egress policy — nothing else.
- **07-21** — 🎯 **MKT01 fully device-verified.** 9 VLAN gateways (exact VLSM masks), uplink `10.255.255.6/30`, loopback, OSPF **Full** (stable 2h+), static default via `10.255.255.5` (OSPF default as backup), `ether2` mgmt-fallback present. Networking done.
- **07-21** — 📄 New living doc: **FGT01 Logging & Flow-Tracing Field Guide** (log→session→debug-flow→sniffer; logs→rules method). Guide reconciliation pass: FGT01 Guide-1 v0.5, SW01 v0.4→v0.5, MKT01 v0.4, 1941 v1.2 (verified 07-20 fixes folded into step bodies; no unverified config).
- **07-21** — FGT01 hardening (Pass 1): `fortigateadmin` (super_admin) + trusthost; default `admin` removed. MFA deferred to Pass-2.
- **07-20** — 🎯 Routed spine complete (OSPF Full). First host on the network via SW01 VLAN-10. FGT01↔1941 /30 live. MKT01 bridge/VLAN/uplink in (`ether3 hw=no`); recovered a power-outage lockout via MAC-WinBox.

## Change Log

> **0.21 (2026-07-29):** re-pointed the plan/order owner to `Operations/Build-Order-and-Dependencies.md` (`ADR-0043` / register E2 — the three-order-docs merge). This tracker is now purely the **execution log**; it no longer restates the plan.

| 0.20 | 2026-07-28 | **B5 (F8/F9): restructured `Current focus`.** Replaced the ~800-word prose block with a pointer to `SESSION-HANDOFF.md` as the single "where we are" owner + a compact Now/Next + a condensed session-snapshot history line. This tracker keeps the execution log; the handoff owns the narrative. |
| Version | Date | Change |
|---|---|---|
| 0.22 | 2026-07-30 | **Reconciled the `Current focus` pointer with `ADR-0049`.** Points at the handoff's pinned **📍 CURRENT STATE** block (dropped the stale `(v10)` ref) and clarifies the split: the **docs page-set replication wave** status = handoff STATE + replication prompt; **this tracker = the *physical-build* execution log**. No execution-log content changed. |
| 0.19 | 2026-07-28 | **Documentation reconciliation session (docs only, no lab).** Recorded ADR-0029 v1.1 (NPS→NPS01), ADR-0030 (DHCP→DC01), ADR-0031 (retire OpenSSL/AD CS-only), ADR-0032 (diagnostics doc architecture). Reconciled `Master-Build-Order`→v1.6 + role/service docs (Device-Role-Assignments v1.1, VM-Inventory, Atlas-Service-Architecture v1.2, SRV01 Guide v0.2 + Checklist v1.2, CA01-VAULT01 v1.1 superseded) + M10/DC02 across IP-plan/303/304/DC-checklist. Current-state here: DC02 → 🟡 operator-reported promoted (read-back pending); CA01 device row struck (retired); PKI standing-blocker "does not retire CA01" → retired. Register → pending v0.8. Historical log/changelog entries below left intact. |
| 0.18 | 2026-07-23 | **AD CS in execution + services shaping.** ICA01 ✅ online `10.20.0.4`; RCA01 (full-GUI clone) patching; DC02 promoting. **FGT auth path resolved → `ADR-0028` (direct LDAPS).** CRL host = **SRV01 (Ubuntu Server/nginx)**, Pi01 reduction preserved; new **Ubuntu golden-image guide `220`** (v0.2). **Microsoft-conformance audit** — AD CS guide → v0.2, `ADR-0027` → v1.2 (only `AlternateSignatureAlgorithm=0` intentional deviation + SHA-256 modernization). `IP-Addressing-Plan` → **v1.1** (host register); **DHCP = Kea/SRV01 (held)** + MKT01-relay; `Master-Build-Order` → **v1.5**. Doc-integrity: a "files reverting" scare was a stale-cache false alarm (git-confirmed all committed + pushed). |
| 0.17 | 2026-07-22 (PM) | **AD CS PKI direction + design.** `ADR-0027` (two-tier AD CS, Microsoft-recommended, working revocation, Tier 0) + new `RCA01-ICA01-ADCS/AD-CS-Two-Tier-Build-Guide.md` v0.1. **Corrected the CA01 device row** (CA01 = OpenSSL intermediate, not AD CS) + added the RCA01/ICA01 row. FGT Pass-2 re-scoped: now **gated on AD CS** (DC LDAPS cert) with the RADIUS-vs-LDAPS auth path still open (`ADR-0004`/`0021`). PAW Win11 golden image **updates complete** → next is sysprep (paused behind AD CS). |
| 0.1–0.3 | 2026-07-20 | Seed → command center (numbered order, connectivity matrix, troubleshooting log). |
| 0.4 | 2026-07-21 | **Restructured to the agreed phased build order** (Networking → Hardening Pass 1 → Services incl. DC/NetBox → Hardening Pass 2 → Phase-7 east-west). Added **PVE01** to the networking foundation and **Phase 2.5**. Marked true status: internet live, FGT01 Guide 1 verified, MKT01 verified, logging field guide, FGT01 Pass-1 in progress. Noted the DC-early divergence (Rule 13). |
| 0.5 | 2026-07-21 | DC progress: **DC01 rebuilt to `atlas.lab`** (was wrong-domain `atlas.local`) — Stages 0–5 device-verified (demote/promote/KDS/DNS/PDCe-time); DC01 guide reworked **GUI-primary** (v0.2). FGT01 Pass-1: default `admin` removed; **MFA deferred to Pass-2**. Added **VM Snapshot & Naming Convention** standard + **per-device Troubleshooting.md** (all 5 devices). CA01 VM cloned (Phase 8, not started). SW01 → v0.5 (PVE01 trunk DAI trust + SSH-KEX). |
| 0.6 | 2026-07-21 | **DC OU skeleton + GPO Stage 7a done.** OU tree built/verified (reserved-name `Computers`/`Users`→`Devices`/`Employees`; `redircmp`→Staging). GPO 7a: MS Server 2025 v2602 baseline imported (`Baseline-ADImport.ps1`, 8 GPOs) + **Wave-A links applied & device-verified** (`gpresult` on DC01 — correct precedence, no lockout; caught/fixed a DC-baseline-at-root mis-link). New living docs `OU-Design-and-Build.md` (v0.2) + `GPO-Design-and-Build.md` (v0.4). Remaining Stage 7: Wave B (VBS/CG — gated on Proxmox VBS check), PSO, LAPS, tier-deny → then DC02. |
| 0.7 | 2026-07-22 | **GPO Stage 7: 7b + 7c + 7c-DSRM device-verified.** Finance/HR PSO (→`G-FinanceHR-Users`, lockout 3), Windows LAPS (schema+self-perm+`LAPS`→Devices; member test deferred), and DSRM-password-via-LAPS on the DCs (`LAPS-DC-DSRM`, fully verified) → **`POL-0002` retired**. `GPO-Design-and-Build.md` → v0.6. Added the **Validation/adversarial-test (pen-test)** backlog item + stubbed `Operations/Validation-and-Adversarial-Testing.md`. Marked 7d tier-deny as gated on Stage-8 tier groups. |
| 0.16 | 2026-07-22 (PM) | **1941 CIS pass complete** (`CIS-Hardening-1941` v1.0 — new) — SSH crypto (CTR/DH2048/retries), documented older-ISR IOS limits (MACs SHA1-only + no `ip ssh server algorithm kex` → `dh min size 2048` mitigation + client `~/.ssh/config` legacy-algo block), vty `access-class` both ranges, Type-9 scrypt secrets, NTP→DC01 (converging), host key stabilized, config saved. **Phase-2.5 step 14 → SW01 + 1941 BOTH ✅ — all three network-device Pass-1/CIS passes done**; only FGT01 Pass-2 AD-LDAPS remains. Device table (1941 CIS row) + focus + blockers updated. ⏳ 1941 NTP re-check `synchronized`. |
| 0.15 | 2026-07-22 (PM) | **SW01 CIS pass complete** (`CIS-Hardening-SW01` v1.0 — new) — SSH crypto, vty access-class, Type-9 scrypt secrets, single admin, NTP→DC01. Phase-2.5 step 14 → SW01 ✅ / 1941 next; device table + focus + blockers updated. |
| 0.14 | 2026-07-22 (PM) | **MKT01 Pass-1 hardening complete** (guide v0.7) + new **`Operations/Device-Hardening-Standard.md`** (shared recovery-first + Pass-1 pattern). Phase-2.5 step 13 → ✅; device table + focus + blockers updated (SW01/1941 CIS + FGT Pass-2 remain). Done log carries the reverse-proxy/cloud/NTP/lockout lessons. |
| 0.13 | 2026-07-22 (PM) | **Stage 8 Part 3 closed out** — seth→`Employees\IT`, `G-IT-Staff`→`Security-Roles`, built-in Administrator rotated+parked (Vaultwarden). Focus/done-log/blockers updated; Stage-8 accounts/groups complete → next is Phase 2.5. |
| 0.12 | 2026-07-22 (PM) | **Backlog capture + PM state.** Troubleshooting log: DC01 mask /19→/26, Protected-Users-RDP (t0-seth NTLM-by-IP), DC02 WinRM/Public-profile, SW01 SSH host-key, and the **PVE native-VLAN-10 VM asymmetry**. Done log: Stage 8 **Part 3 accounts** verified (open: seth-move + §3e), **PAW01 guide** + Win golden image start, **Master-Build-Order v1.3**. Focus + standing blockers updated (Phase 2.5 attention, FGT Pass-2 unblocked, git push, open Stage-8 items). |
| 0.11 | 2026-07-22 | **Stage 8 Part 2 (AGDLP groups) device-verified** — `G-Tier0/1/2-Admins`+`G-IT-Staff` all Global/Security, tier groups in correct OUs; 7d now has real groups to deny. Placement nit on `G-IT-Staff` (Groups-root → recommend Move to Security-Roles). Done-log + focus + device table updated; Tiered-Admin → v0.4, Checklist → v1.5. |
| 0.10 | 2026-07-22 | **Tiered-Admin doc → v0.3 GUI-first** (every step an ADUC/ADAC click-path; PowerShell optional). IT-OU decision locked to `OU=IT,OU=Employees`. Version refs synced in the device table, focus, checklist, handoff. |
| 0.9 | 2026-07-22 | **DC02 split into `DC02-Build-Guide.md` v0.1** (dedicated replica-DC living guide). Tiered-Admin doc → v0.2 (Part 4 now a pointer); `DC01-Build-Guide.md` → v0.5; Build-Checklist → v1.4. Device table gains a DC02 row. All 📋 authored-not-executed (POL-0001). |
| 0.8 | 2026-07-22 | **Stage 8 authored (not yet executed).** New living doc `Tiered-Admin-and-Groups-Build.md` v0.1 — AGDLP tier admin groups (`G-Tier0/1/2-Admins`) + tiered accounts (`t0-seth`/`t1-seth`/`seth`) + getting off the built-in Administrator + Protected Users + DC02 promotion/replication. Marked 📋 authored-awaiting-execution across the tracker (focus, step 15, device table, 7d blocker) per POL-0001. Concretized `GPO-Design-and-Build.md` §7d group names (v0.7); Build-Checklist → v1.3. |
