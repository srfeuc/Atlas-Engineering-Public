---
Title: Lab-02 Cleanup & Readability — Flag Register (WORKING)
Path: Labs/Lab-02-Cisco-Core
Status: 🟠 WORKING — review artifact (Seth + assistant, 2026-07-27). **Re-baselined against the current tree** after a `git pull` revealed this clone was 4 commits behind. **Scope refocused:** the pre-existing `Operations/Doc-Conflict-Audit-2026-07-24.md` + `Foundation-Doc-Conflict-Audit-2026-07-24.md` own the *stale-doc conflicts*; THIS register owns the **new forward decisions, the learnability/Academy layer, and device/ops-level flags those audits didn't scope.** Flag-first; nothing changed until AGREED + actioned. **v0.7 (2026-07-28): Phase 1 ADRs drafted** — `ADR-0030` (DHCP on DC01, captures A1) + `ADR-0031` (retire OpenSSL Lab CA, captures A3) authored this session. **v0.8 (2026-07-28): committed + Phase-2a cascade complete** — ADR-0030/0031/0032/0033 all committed; Master-Build-Order → v1.6 and every role/service doc reconciled; **C1/C2/C3 closed**; all 32 ADRs given a **Scope** + an index (`ADR-0033`). Remaining = Phase-2 loose ends (A2a/A2b/A4a, C4–C7, B1–B5, per-device `Diagnostics.md` seeding, D5 lab). **v0.9 (2026-07-28): A2a/A2b/A4a + C4/C5/C6 closed; found+fixed the A1 IP-plan DHCP gap.** **v0.10 (2026-07-28): C7 CLOSED — `ADR-0034` decides PVE01-networking ownership.** **v0.11 (2026-07-28): B2/B3/B4/B5 closed + per-device `Diagnostics.md` seeded for all 8 built devices.** **v0.12 (2026-07-28): D6 ACCEPTED + Academy `Command-Library/` built.** **v0.13 (2026-07-28): B1 CLOSED — the whole B-series (B1–B5) is done.** **v0.14 (2026-07-28): planning session — decisions closed (`ADR-0035` FGT no-UTM · `ADR-0036` compute topology/2nd-Proxmox · Vaultwarden→BKP01 closes A3a · gap-analysis Tier-A committed) + a planning package produced (Master-Implementation-Checklist, Service-Server-Build-Plan, SoT-Evidence-Run-Sheet, NetBox-Data-Load-Prep, Public-Release-Manifest).** **v0.15 (2026-07-28): Documentation standard ADOPTED (`ADR-0037`) - per-device/service doc architecture; governing doc `Atlas-Documentation-Standard.md` (v1.0) authored; ADR-Index v1.3; D8 logged.** **v0.16 (2026-07-28): Planning-doc sprawl logged as E1 (Section E) - overlapping planning/status docs can contradict; the Wave-B estate roadmap will be the single source; consolidation approach = operator decision.** **v0.17 (2026-07-29): E1 Wave-B execution — model = **move-alongside-devices**; 5 committed roles (FS01/WSUS01/SQL01/RDS01/SIEM01-Wazuh) given `Devices/` stub homes + README front-doors; the estate single-source (`Service-Server-Build-Plan`) got the Lab-01 `006` authority **Rule**; TrueNAS deferred, PBS01=BKP01. Logged **E2**: consolidate the 3 build-order docs (Lab-01 single-doc model) + backfill device READMEs.** **v0.18 (2026-07-29): pfSense IDS/IPS logged as a future security role in the estate (fills the *prevention* gap vs the passive Suricata-on-SPAN IDS; topology ADR required when committed). Operator's `BATlogin.txt` preserved as an Academy concept module.** **v0.19 (2026-07-29): Restructure kicked off (E2) — **DC-Domain-Controllers** built as the worked exemplar of the per-device model (README front-door + connections map, `Roadmap.md`, `Considerations.md`) over the existing verified docs; the `Roadmap.md` doc-type + connections-map requirement codified into `Atlas-Documentation-Standard`/`-Workflow` v1.1; ADR-0037 amendment pending.** **v0.20 (2026-07-29): ADR-0037 AMENDED (v1.1) + ADR-Index v1.4 — the `Roadmap.md` doc-type + connections-map requirement are now formal; the five-step propagation is complete and the pending-propagation flag is CLOSED.** Remaining = bench work (D5 lab, DC02 read-back, AD CS ceremony), public-repo publish (dedicated session), Confluence redirect, command-library expansion.
Version: 0.35
Date: 2026-07-30
---

# Lab-02 — Cleanup & Readability Flag Register (refocused)

> **Relationship to the 2026-07-24 audits (POL-0008, one source of truth):** a prior session already flagged the Architecture / Windows-Infrastructure / Foundation *doc-vs-current-design* conflicts, re-verified, with severities + a reconciliation order. We **do not restate those** — Section D maps our overlapping flags to theirs. This register's unique value is **Sections A–C**: the decisions that *change* the design, the learnability layer, and the **Devices/Operations pages the 07-24 audits explicitly did NOT scope** ("Devices pages treated as current, not audited").
>
> **Status key:** 🔵 OPEN · 🟡 DISCUSSING · 🟢 AGREED · ✅ DONE/N-A · ⚪ WON'T-DO. **Types:** decision-ripple · nav-readability · needs-info · knowledge-transfer.

## Current-design baseline (the yardstick, per the 07-24 audits + our decisions)
- **Trunks:** native **VLAN 999** (parking); management **tagged VLAN 10** everywhere (incl. PVE `vmbr0.10`). *(The old "native VLAN 10 / VLAN-10 VMs break" material is inverted/stale.)*
- **Auth:** **ADR-0029 (07-24) already retired FreeRADIUS** → network devices = **NPS**; FGT = LDAPS (`ADR-0028`); certs from AD CS (`ADR-0027`). ADR-0029 placed NPS **on the DC**; **D7 moved it to a dedicated member server `NPS01`** — ✅ **ADR-0029 v1.1** (2026-07-28) + new `Devices/NPS01-Network-Policy-Server/` page.
- **DHCP:** 07-24 design = **Kea on SRV01**; **our D1 moves it to DC01** (this *inverts* the 07-24 "DHCP-on-a-DC is wrong" findings). ✅ **ADR-0030** (2026-07-28, drafted): fewer-VMs driver; failover DC01-now/DC02-later; dedicated DDNS account, **no `DnsUpdateProxy`**.
- **PKI:** 07-24 design = two-tier **AD CS (domain)** + **OpenSSL (non-domain)** coexist; **our D4 retires OpenSSL.** ✅ **ADR-0031** (2026-07-28, drafted): reverses `ADR-0003`; fold Pi-hole/MKT01/FGT01 onto AD CS; **CA01-VAULT01 decommissioned entirely** (Vaultwarden relocates — see revised C3).

## 0. Decisions

| # | Decision | Direction | Status |
|---|---|---|---|
| D1 | DHCP server | **DC01** (not Kea/SRV01). *Inverts* the 07-24 audits' DHCP findings → its own ADR. | ✅ DONE — **ADR-0030** (committed `b24b1be`); Phase-2a doc-rows flipped to DC01 |
| D2 | Network-device auth | **NPS**, FreeRADIUS retired. **Already recorded — `ADR-0029` (07-24).** | ✅ DONE (ADR-0029) |
| D3 | Pi01 role | DNS + NTP only. Confirmed by Master-Build-Order Phase 5 + ADR-0029. | ✅ DONE (design) |
| D4 | Retire OpenSSL Lab CA | Consolidate on AD CS; reverses `ADR-0003`. | ✅ DONE — **ADR-0031** (committed `b24b1be`); Phase-2a PKI reversal done |
| D5 | Retirement method | Time-boxed migrate-and-test lab (CA half ~2/3 pre-written). | 🟢 AGREED |
| D6 | Atlas Academy | Adopt as "why it works" layer; link into 303/304. | ✅ **DONE — D6 ACCEPTED** (operator, 2026-07-28). Academy adopted: `Concepts/` (B2/B3) + `Command-Library/` (platform-first verify reference, `ADR-0032`) + house-style (B4). README → Adopted. |
| D7 | NPS host | **Dedicated Windows member server `NPS01`** (not the DC). **Amended `ADR-0029` → v1.1.** Reuses a spare Windows VM. Unblocks the deferred member-server LAPS tests. | ✅ DONE — **ADR-0029 v1.1** (2026-07-28) + NPS01 page |
| D8 | Documentation standard | Adopt the per-device/service doc architecture (Lab-01 pattern generalized) + author-in-order lifecycle (checklist-first) + Roles/ per service on multi-service hosts. | ✅ DONE - **ADR-0037** (2026-07-28); governing doc `00-Atlas-Foundation/Atlas-Documentation-Standard.md` |
| D9 | Build discipline | **Incremental, test-gated implementation** — one unit at a time; each unit's acceptance gate (positive + negative test, `POL-0006` evidence) passes before the next; ✅ only on a passing gate; adversarial gates live in `Operations/Validation-and-Adversarial-Testing.md`. Elevates the MKT01 east-west one-rule-at-a-time pattern estate-wide. | ✅ DONE — **ADR-0041** (Global, 2026-07-29) |

## Section A — New-decision ripples (ours — the audits didn't make these calls)

| ID | Dec | Flag | Status |
|---|---|---|---|
| A1 | D1 | **DHCP→DC01 inverts the 07-24 findings.** Their Lab-02 **M8** + Foundation **H1** flag "DHCP as a DC role" as the *error* (authoritative = Kea/SRV01). Under D1 the polarity flips: DC01 becomes correct, and the **Kea/SRV01** wording (`IP-Addressing-Plan-VLSM`, `Atlas-Service-Architecture`, `Lab-02-Device-Role-Assignments`, `VM-and-Services-Inventory`, SRV01 docs) is what changes. Relay stays on MKT01 → target DC01. Record the DnsUpdateProxy/Tier-0 trade-off + DC01/DC02 failover in ADR-0030. | ✅ **captured in ADR-0030** (drafted 2026-07-28): fewer-VMs driver, DC01-now/DC02-later failover, dedicated DDNS account + **no `DnsUpdateProxy`**, role-separation trade-off reconciled vs `ADR-0027`/`0029`. Doc-row flips → **Phase 2a ✅ DONE** (2026-07-28): Kea/SRV01 wording flipped to **DC01** across Service-Architecture, Device-Role-Assignments, VM-Inventory, SRV01 guide+checklist, Master-Build-Order v1.6. 🔴 **Gap found + fixed v0.9 (2026-07-28):** the **IP-Addressing-Plan-VLSM** DHCP wording was still "Kea on SRV01" at HEAD (its own v1.3 changelog had explicitly deferred that flip) even though v0.8 listed the IP-plan as flipped — now corrected to **DC01** in **IP-plan v1.4**. |
| A2 | D7 | **Amend ADR-0029:** its Decision + Consequences said NPS "on the DC." ✅ **Done — ADR-0029 v1.1** (member server `NPS01`; availability caveat + break-glass retained; MS NPS-on-DC perf note logged as the rejected alternative). NPS01 given a **placeholder device page**. **Estate reconciliation → Phase 2** (see Section A2-ripples). | ✅ amendment DONE; estate → Phase 2 |
| A3 | D4 | **Reverses the 07-24 PKI reconciliation.** Their audits reconcile docs *toward* AD CS+OpenSSL coexist (Lab-02 M1–M6). ADR-0031 reverses `ADR-0003`; then the AD-CS guide needs the **non-domain enrollment/trust steps** it lacks, CA01's OpenSSL role is struck, and **CA01-VAULT01 splits** (Vaultwarden survives → AD CS cert). `ADR-0027` explicitly says D4 needs its own ADR. | ✅ **captured in ADR-0031** (drafted 2026-07-28): reverses `ADR-0003`; `ADR-0009` reissue cost stated; **decommission CA01-VAULT01 entirely** (Vaultwarden relocates — revises C3, new ripple below); migrate-and-test (D5) before retiring. Non-domain enrollment/trust guide steps → **A4a ✅ DONE 2026-07-28** (AD-CS guide **Part 3B** added). Doc-preamble reversal → **Phase 2a ✅ DONE** (2026-07-28): PKI narrative reversed to **AD-CS-only** across Service-Architecture/VM-Inventory/Device-Role/Master-Build-Order; CA01-VAULT01 superseded. |
| A4 | D2/D7 | NPS **PEAP server-cert** is a build-time prereq once the member server exists (was "deferred" in the AD-CS guide, tied to old ADR-0004). ✅ **Recorded build-time in ADR-0029 v1.1 + the NPS01 page**; the AD-CS-guide edit itself is **A4a ✅ DONE 2026-07-28** (guide **§3.5**). | ✅ DONE — recorded in ADR-0029 v1.1; AD-CS-guide §3.5 build-time (2026-07-28) |

## Section A2-ripples — D7 estate/reconciliation follow-on (spawned by the ADR-0029 v1.1 amendment → Phase 2)

| ID | Doc | Change | Status |
|---|---|---|---|
| A2a | `Architecture/IP-Addressing-Plan-VLSM` | Add the **NPS01** host row — **`10.20.0.12`**, VLAN 20 **server range** (the Tier-0 `.2–.9` carve stays CAs+DCs only). Authoritative address home (`POL-0008`). | ✅ **DONE** (2026-07-28): NPS01 row added, IP-plan v1.4. |
| A2b | `Architecture/Atlas-East-West-Allowed-Flows-Matrix` | Add flow: **MKT01/SW01/1941 → NPS01, UDP 1812/1813 (RADIUS)**. Deliberately keeps NPS **out** of the `.2–.9` Identity micro-zone (flow #9) so that zone stays LDAPS/Kerberos/DNS-only. | ✅ **DONE** (2026-07-28): flow #14 added, matrix v1.2; flow #9 annotated. |
| A2c | `Architecture/Atlas-Service-Architecture` + `Lab-02-Device-Role-Assignments` | Add an **NPS01** service/role row (rides along with the SRV01 role-row fixes already in the 07-24 order). | ✅ **DONE** (2026-07-28): NPS01 rows added to both. |
| A4a | `Devices/RCA01-ICA01-ADCS/AD-CS-Two-Tier-Build-Guide` | Deferred "NPS server cert … when NPS is stood up" (line ~308) → **build-time** (RAS-and-IAS-Server template from ICA01). | ✅ **DONE** (2026-07-28): guide v0.3 — §3.5 build-time NPS cert **+ Part 3B** non-domain enrollment/trust (Pi-hole/MKT01/FGT01) per `ADR-0031`. |

> C4 already carries the CIS-Hardening 1941/SW01/MKT01 wording (now "NPS on **NPS01**"). The new **`Devices/NPS01-Network-Policy-Server/Build-Guide.md`** (v0.1 placeholder) exists — expand to a full Guide + Checklist when NPS01 is built.

## Section B — Learnability / Academy (ours — outside the 07-24 audit scope)

| ID | Type | Flag | Status |
|---|---|---|---|
| B1 | nav | **No single "start here" + no glossary.** 4 competing front doors (root README stale, Blueprint pre-restructure, PORTFOLIO, Foundation README). Highest-leverage learnability fix. *(prev F10/F37)* | ✅ **DONE** (2026-07-28): root `README.md` rewritten as **the** front door (start-here-by-need table, repo map, conventions, the front-door hierarchy) + new **`GLOSSARY.md`** (acronyms + Atlas terms + devices). The 3 other front doors got role banners: **Blueprint** → historical planning snapshot, **PORTFOLIO** → reviewer/showcase entry, **Foundation README** → Foundation index. Each points at the root README + handoff. |
| B2 | knowledge-transfer | **Tiered-Admin doc assumes concepts** → the comprehension gap you hit. Fix = 303 Parts 4-5 cross-link + a **tier-model one-pager** (Academy). *(F17)* | ✅ **DONE** (2026-07-28): `Atlas-Academy/Concepts/Tiered-Admin-Model.md` (4-part module) + a cross-link blockquote in **303 Part 5**. |
| B3 | knowledge-transfer | **Adopt Academy** (D6); link into 303/304, don't duplicate. Uncovered-concept targets — Windows: **FSMO, DFSR/SYSVOL, SCT baselines, VBS/CG, DSRM**; Network: **OSPF learn-vs-originate, DAI trust, RouterOS-v7**. *(F29/F46)* | ✅ **DONE** (2026-07-28): Academy adopted (README → Adopted); `Concepts/README.md` index **seeds all 8 targets** (W1–W5 + N1–N3) with Atlas examples + sources; device `Diagnostics.md` pages link up into it. |
| B4 | knowledge-transfer | **Preserve as house style:** GPO Part-1 mental model; PSO §7b "measure first"; DC02 "what a 2nd DC doesn't buy"; FGT Logging field-guide. *(F30)* | ✅ **DONE** (2026-07-28): `Atlas-Academy/Atlas-Teaching-Patterns-and-House-Style.md` — the four patterns named with their exemplar docs + the standing conventions. |
| B5 | nav | Tracker "Current focus" = one ~800-word para; restructure. Consolidate "where we are" to one owner. *(F8/F9)* | ✅ **DONE** (2026-07-28): tracker v0.20 — `Current focus` restructured to a pointer (`SESSION-HANDOFF.md` = the one owner) + compact Now/Next + condensed history. |

## Section C — Device / Operations flags (ours — the 07-24 audits did NOT scope Devices, and Operations only partially)

| ID | Dec | Flag | Status |
|---|---|---|---|
| C1 | D2/D3/D4 | **`Device-Backup-Runbook` §4 + Pi01 checklist still back up `/etc/freeradius` + `/etc/ssl/lab-ca`** — dead paths after retirement; a backup pointing at nothing fails silently on restore. **Do early (safety).** *(F42)* ⚠️ re-verify against current text. | ✅ **DONE** (2026-07-28): `Device-Backup-Runbook` v1.1 — dead `/etc/freeradius` + `/etc/ssl/lab-ca` dropped from the steady-state tar, silent `2>/dev/null` → `-v`, gated **pre-migration** full backup added (keeps CA/RADIUS/Vault until migration confirmed). Pi01 checklist was **already** the reduced DNS+NTP build — no change. |
| C2 | D1/D2 | **SRV01 Build-Guide + Checklist still own Kea DHCP + FreeRADIUS** (ADR-0029 §Consequences lists these as pending reconciliation); **Ubuntu(guide) vs Debian(checklist) OS split.** *(F39)* ⚠️ SRV01 checklist changed 07-24 (native-VLAN) — re-verify. | ✅ **DONE** (2026-07-28): SRV01 Build-Guide v0.2 + Checklist v1.2 — **Kea DHCP struck** (→ DC01) and **FreeRADIUS struck** (→ NPS01); SRV01 = nginx-CRL/Oxidized/TFTP-SFTP/rsyslog. (OS split was already resolved to Ubuntu, checklist v1.1.) |
| C3 | D4 | **CA01-VAULT01 joint doc:** strike CA01 OpenSSL build; **Vaultwarden survives** → own doc + AD CS cert; "VAULT01-first (holds CA passphrases)" rationale weakens. *(F41)* 🔁 **REVISED by ADR-0031:** host is **decommissioned entirely** (not renamed/kept in place); Vaultwarden **relocates** off it → see new ripple **A3a**. | ✅ **DONE** (2026-07-28): `CA01-VAULT01/Build-Checklist` v1.1 SUPERSEDED — CA01 struck, host decommissioned. Vaultwarden relocation tracked as **A3a**. |
| **A3a** | D4 | 🆕 **Vaultwarden relocation (spawned by ADR-0031's "decommission entirely").** Vaultwarden survives as an independent service but needs a **new surviving host**, an **AD CS (ICA01) cert**, a **DB restore**, and it **retains its `ADR-0009` secrets-custody role** (RCA01/CA backup passphrases, DSRM, built-in-Administrator break-glass). | ✅ **DONE** (2026-07-28): **host = BKP01** (`ADR-0036`, operator) — Vaultwarden co-locates on the backup host (→ **PVE02** for host-loss survivability), standalone web console; addr **`10.20.0.13`** proposed; AD CS (ICA01) cert + DB restore remain build-time; 🔴 master-password recovery (`049`) still owed before trusting the vault. |
| C4 | D2 | **CIS-Hardening 1941/SW01/MKT01** Pass-2 = "RADIUS/TACACS+ → SRV01/NPS" → **NPS on the member server**; MKT01 "AD-LDAPS admin" is wrong (→ NPS); SW01 "Pi01 = RADIUS host" stale. Break-glass survives. *(F43)* | ✅ **DONE** (2026-07-28): CIS-1941 v1.1, CIS-SW01 v1.1, CIS-MKT01 v1.2 — Pass-2 auth = **RADIUS→NPS on `NPS01`** (`ADR-0029`); MKT01 "AD-LDAPS admin" corrected to RADIUS-client; `ADR-0004`→`ADR-0029` refs. Also fixed MKT01 Build-Guide v0.8 (same stale wording). |
| C5 | D2/D4 | **FGT CIS + Build-Checklist predate ADR-0028** (local-admin suggestion; "RADSEC" leftover; not routed to Guide-2b). **Guide-2b itself is clean.** FGT Index omits Troubleshooting + Logging. *(F44)* | ✅ **DONE** (2026-07-28): CIS-FGT01 v1.2 + FGT Build-Checklist v1.1 routed admin auth to **`ADR-0028`/Guide-2b** (LDAPS; `fortigateadmin` local break-glass), **struck the RADSEC leftover**; FGT Build-Guide-Index v0.3 adds Troubleshooting + Logging. |
| C6 | — | **MKT01 two overlapping Phase-7 firewall docs**; Troubleshooting cites stale Build-Guide versions. *(F45)* | ✅ **DONE** (2026-07-28): added a "which doc is which" disambiguation note to `Firewall-Rebuild-and-Per-Rule-Verification-Plan` (verification method vs the worksheet's build sequence — they don't overlap); MKT01 `Troubleshooting` v1.1 stale **Build-Guide (v0.4)→(v0.8)**. |
| C7 | — | **Virtualization freeze blockers** (guide-number collision, PVE01 3 homes, thin records); **Validation-and-Adversarial-Testing** is an honest v0.1 stub. *(F47/F48)* | ✅ **DONE** (2026-07-28): manifest v1.2 — **both** freeze blockers resolved: #1 numbering (guides already `2xx`) + **#2 ownership via `ADR-0034`** — new authoritative **`Build-Records/PVE01-Networking.md`** (consolidates the current device-verified `/27`/tagged-`vmbr0.10`/native-999 design; the frozen Lab-01 `Build-Record-Network`/`Build-Guide-Network` were the *stale* `/24`/native-10 owners → now pointers; `204` = procedure; **Confluence needs a manual redirect**). Residual (non-blocking backlog): storage/auth Build-Records + the honest Validation v0.1 stub. |
| C8 | — | 🆕 **RDS01 host placement — user-facing availability vs power (2026-07-30, replication Batch A).** The estate index + handoff place RDS01 on **PVE01/R410** (mostly-off spin-up; rationale *session host is RAM-heavy, non-critical*). But RDS serves **standard users** — if the host is off, published desktops are unavailable. For the **#20 compute/sizing sweep**: does user-availability warrant the **always-on EQR6** instead? (`ADR-0036` owns placement; `POL-0008`.) Carried in `Devices/RDS01-Remote-Desktop/Considerations.md`. | 🟢 **DECIDED (operator 2026-07-30) → PVE02/EQR6 always-on** (user-facing availability). Gateway/Web **included**; VLAN **20** confirmed. Residual: RAM sizing → **#20**. |

## Section C′ — D5 migrate-and-test lab (ours)
- **Lab-01 CA not rebuildable** (missing `openssl.cnf`, both tiers); 2 off-book unrevocable certs; **RADIUS never device-tested** (localhost only). *(F12–F14)* — all closed by the D5 lab.
- **~2/3 pre-written:** reuse `CA-Migration-and-DR-Lab` + `CA-Migration-Handoff-Sanity-Check` (has the openssl.cnf `.bak`/SAN trap) + `CA-PKI-Recovery-Objectives-RPO-RTO`. Add the **FreeRADIUS→VM move + one real device→RADIUS login**. New **`Device-Confirmation-Commands.md`** (07-24) supports the "confirm before decommission" step ADR-0029 requires. *(F28)* | 🔵 OPEN

## Section D — Deferred to the 2026-07-24 audits (do NOT restate; map only)

| Our earlier flag | Now tracked by | Note |
|---|---|---|
| F20 (DHCP Kea/SRV01) | Lab-02 **M8**, Foundation **H1** | Polarity **flipped** by D1 → see A1 |
| F21–F23 (NPS/FreeRADIUS/FGT-RADIUS) | Lab-02 **H2/M7**, Foundation **M9** | Resolved by **ADR-0029**; residual wording in their reconciliation list |
| F24–F26 (OpenSSL coexist) | Lab-02 **M1–M6, L3, L4** | Direction **reversed** by D4 → see A3 |
| F31 (Windows build order dup) | (nav — not in their audit) | keep as B-adjacent nav; low priority |
| F32 (service-ownership dup) | Lab-02 **meta-finding** (NetBox = SoT) | their reconciliation covers it |
| F33 (303 domain bug) | Lab-02 **M14** | ✅ **already fixed** (commit `78679f3`) |
| F34 (303 H1 "Roadmap") | (minor) | fold into their 303 reconciliation |
| F35 (POL-0005 "missing") | — | ✅ false; POL-0005 exists |
| F36 (DC01-LAB reversed) | Lab-02 **M5** | defer |
| F38 (Foundation contradictions) | **entire Foundation audit** | defer; D1/D4 change some directions |

## Section E — Planning-doc reconciliation (ours; raised by operator 2026-07-28)

The estate has several overlapping **planning/status** docs that can drift out of agreement. Not a content error today — a **navigability + single-source risk** the operator flagged: the docs are liked, but they get confused and should be easier to follow. Tracked here so Wave B handles it by design (not another competing doc).

| # | Flag | Notes | Status |
|---|---|---|---|
| E1 | Planning/status doc sprawl | `Master-Build-Order`, `Master-Implementation-Checklist`, `Build-Progress-Tracker`, `Service-Server-Build-Plan`, `Architecture/Atlas-Service-Architecture`, `Architecture/Lab-02-Device-Role-Assignments` (+ the wider `Architecture/` set) overlap on *what gets built, in what order, and current status* and can contradict. Open question: whether some belong **alongside the devices**. **Proposed approach:** the Wave-B estate roadmap/inventory becomes the **single source** for *what exists + status*; each other doc declares a narrow owned-scope in its header and links to it (`ADR-0037` fact-ownership map) instead of restating. Consolidate vs annotate-in-place vs move-alongside-devices = **operator decision** at Wave B. | 🟡 IN PROGRESS (Wave B 2026-07-29) — decisions taken; see *E1 — resolution* below |
| E2 | Consolidate the 3 build-order/status docs → one (Lab-01 model) + backfill device READMEs | Lab-01 solved E1's core with a **single** `Operations/Build-Order-and-Dependencies.md`; Lab-02 has **three** overlapping (`Master-Build-Order` + `Master-Implementation-Checklist` + `Build-Progress-Tracker`). Also: backfill `README.md` front-doors on the 14 existing `Devices/` folders; consider Lab-01's numbered Architecture reading order (`001`–`006`). | ✅ **DONE** (2026-07-29, `ADR-0043`) — `Operations/Build-Order-and-Dependencies.md` authored (the single owner); `Master-Build-Order` + `Master-Implementation-Checklist` → superseded pointers; `Build-Progress-Tracker` → execution log; `Atlas-Service-Architecture` → design scope banner. Device-README backfill continues per wave. |

### E1 — resolution (Wave B, 2026-07-29)

**Decisions taken (operator):** reconciliation model = **move-alongside-devices**; the aspirational roles were split **build vs defer** — **build** FS01, WSUS01, SQL01, RDS01, SIEM01/Wazuh (SRV01 = 'the services server', already authored); **defer** TrueNAS (**PBS01 = BKP01**, product name, not a separate host).

**Executed this session:** (1) the estate single-source `Service-Server-Build-Plan.md` gained an authority **Rule** header (Lab-01 `006-Network-Source-of-Truth` pattern — *update here first; the other planning docs link, they do not duplicate*) + the 5 committed roles in the estate table + a *Wave-B committed roles* section + a deferred note; (2) each committed role got a **`Devices/` stub home** (`Build-Checklist.md`, NPS01-stub style) **+ a `README.md` front-door** (Lab-01 device-README pattern); (3) `Architecture/Lab-02-Device-Role-Assignments.md` VM-estate table reconciled from the generic `FS01/WSUS01/WS01` row to the committed set.

**Lab-01 patterns adopted as the E1 model (operator nudge 2026-07-29):** Lab-01 solved this exact sprawl with a **single** `Build-Order-and-Dependencies` doc, a numbered `Architecture/001–006` reading order, per-device `README.md` front-doors, and an authority *Rule* on each source of truth. Wave B has now applied the Rule + device-README patterns; the doc-consolidation is logged as **E2** (bigger restructure, needs operator go-ahead).

## Notes
- **⚠️ Delta-file caveat:** flags touching files changed in commits `de76981..78679f3` (Master-Build-Order, IP-Addressing-Plan, 303, SRV01 checklist, SW01 guide, NETBOX, 220, 204) were partly raised against the pre-pull clone — **re-verify against current text before actioning.** All other reads match the current tree.
- **07-24 audit reconciliation order (theirs, still valid):** before NetBox — SRV01/CA/DHCP role rows + MON01 addr; build-blocking — OU names, FGT LDAPS, native-999; PKI narrative preamble; then cleanups.

## Change Log
| Ver | Date | Change |
|---|---|---|
| 0.35 | 2026-07-30 | **#20 compute-placement + sizing reconciliation (session 18, estate-wide rules first).** Operator opened the #20 sweep — "where do the VMs live (PVE01 vs PVE02), resolve the drift, update the estate-wide rules first" (the handoff + owner docs predated `ADR-0036` v1.2 / `ADR-0034`). **Decisions:** DC02 stays **PVE01/R410** (reaffirms `ADR-0036` principle 1 — DCs on different physical hosts; the index had co-located both DCs on the EQR6); placement + sizing single source = **`Service-Server-Build-Plan`**; **`VM-and-Services-Inventory` RETIRED**. **Edits:** `Service-Server-Build-Plan` **v1.6** (authority + 3 reconciled cells [DC02→R410, ICA01+SRV01→EQR6] + the **EQR6 RAM budget** [64 GB holds ~44 GB always-on incl. RDS01/WAC01/PAW01 → resolves the RDS01-RAM Q] + the single-8 TB SPOF note) · `IP-Addressing-Plan-VLSM` **v1.9** (VLAN-10 rule now names **both** hypervisor hosts; PVE01 `10.10.0.10` + **PVE02 `10.10.0.11` 📋** registered; host placement pointed to the index) · `VM-and-Services-Inventory` RETIRED banner · `ADR-0036` **v1.3** · `ADR-Index` **v1.21** · new **`Operations/Compute-Placement-Reconciliation.md`** (the #20 decision record) · `SESSION-HANDOFF` **v31** (session-18 block). **Residual #20 (address/VLAN deconflicts — next slice):** PAW+WAC01 on VLAN 10 + the VLAN-10 low-block deconflict (WAC `.5`/Pi01 `.6`/PVE02 `.11`); SIEM01 VLAN 40-vs-20 + indexer RAM; CNT01 sizing; Pi01 VLAN-10 DNS/NTP-ingress. Docs-only; pending Seth commit. |
| 0.34 | 2026-07-30 | **Replication wave COMPLETE + close-out.** Batch B (NETBOX01·BKP01+Vaultwarden·Pi01·CNT01) + Batch C+D (1941·SW01·MKT01·FGT01·PFSENSE01·KALI01·SIEM01) page-sets DONE → **every device now has its standard page-set**. New **Services-map** README element (`Atlas-Documentation-Standard` **v1.7**; backfill of the existing READMEs = **#27**, high-priority). FGT01 **K1/K2 decided** + **K3 FSSO** written up as Academy concept **N4** (+ Backlog **#26**); **`ADR-0038` v1.2** pfSense sub-decisions (physical 2-NIC · **fail-closed** + manual transit-bypass break-glass · monitor-first · Suricata); **SIEM01 = dedicated host** (Wave-B flag closed); **DC01 → PVE02/EQR6** + **ICA01 → EQR6** (`ADR-0036` v1.2 — no `CM-0012` CMOS risk). **Close-out (this session):** two new Academy labs — `Atlas-Academy/Concepts/Proxmox-VM-Migration-and-Host-Bring-Up.md` (**V1**, migrate VMs onto EQR6) + `Concepts/Ansible-IaC-Device-Provisioning.md` (**A1**, IaC/Ansible on Proxmox/Hyper-V) → Concepts index **v1.2**; `00-Atlas-Foundation/Contributing-Adding-Docs.md` now points device-folder *contents* at Standard v1.7; `SESSION-HANDOFF` → **v30** with a **#20/#21 NEXT-SESSION brief**. Docs-only; pending Seth commit. |
| 0.33 | 2026-07-30 | **WAC01 page-set ✅ → replication Batch A COMPLETE.** Full page-set under `Devices/WAC01-Windows-Admin-Center/` (Standard v1.6). Operator decisions: **PVE02/EQR6 always-on** + **VLAN 10 (management)** (WAC is a Tier-0 admin surface — exercises `ADR-0045`'s VLAN review trigger, **overrides its VLAN-20/PVE01 defaults**) + **Azure Arc gated stub**. Propagated: estate index v1.3 · IP-plan v1.6 (WAC01 `10.10.0.5` VLAN 10 📋) · flows-matrix v1.4 (**flow #16** PAW→WAC 443 + WAC→estate WinRM) · build-order v1.2 · `ADR-Index` v1.17 (ADR-0045 annotated). 🔴 **Flag:** PAW-VLAN drift (IP-plan text VLAN 10 vs estate index VLAN 20) — settle both admin surfaces together in #20; deconflict WAC's `.5`. Docs-only; pending Seth commit. **Next: Batch B, fresh session.** |
| 0.32 | 2026-07-30 | **`ADR-0049` — Documentation-Session Planning & Handoff Protocol adopted** (operator; formalizes the RDS01 planning pass). Ask-design-questions-at-planning norm · **handoff → Living STATE block + append-only log with a read rule** (archive to `99-Archive` past ~8 blocks/~80 KB) · per-device narrowing · **Standard/Workflow → v1.6** (connections-diagram **edges labelled with protocol/port**; `ADR-0037` amended) · replication prompt → v1.4 (§2a) · `ADR-Index` v1.16. RDS01 README diagram re-labelled as the first v1.6 example; edge-label backfill of the other 8 READMEs → #22. **Reconciled the `Build-Progress-Tracker` `Current focus` pointer** to the handoff STATE block (`POL-0008`: handoff = where-we-are, tracker = physical-build execution log; fixed the stale `(v10)`). Docs-only; pending Seth commit. |
| 0.31 | 2026-07-30 | **RDS01 operator decisions + propagation (Batch A).** C8 **DECIDED → PVE02/EQR6 always-on**; **RD Gateway/Web included**; **VLAN 20** confirmed (client-reached service workload, the IP-plan NetBox rationale). Propagated: estate index host cell → EQR6; **IP-plan v1.5** (RDS01 register row `10.20.0.17` 📋); **flows matrix v1.3** (new **flow #15** Clients→RDS01 gateway 443 + host 3389); RDS collection access grounded to the AGDLP model (dept global / `G-IT-Staff`, never `G-Tier0-Admins`). **Logged Backlog #23** — a test-workstation fleet (a few Win11 VMs + light Linux clients, VLAN 50) to exercise users/RDS/E-W flows. Docs-only; pending Seth commit. |
| 0.30 | 2026-07-30 | **RDS01 page-set ✅ (replication Batch A) + placement flag.** Full standard page-set authored under `Devices/RDS01-Remote-Desktop/` (Standard v1.5) → estate index `Service-Server-Build-Plan` **v1.2** (status flipped *committed — stub → not built · page-set ✅*) + `Device-Page-Set-Replication-Prompt` **v1.3** (RDS01 ✅; **Batch A: 1 left → WAC01**) + `SESSION-HANDOFF` **v25**. **Logged C8** — RDS01 host placement (user-facing availability vs the R410 spin-up model) → **Backlog #20**. Docs-only; pending Seth commit. |
| 0.29 | 2026-07-30 | **Doc Standard → v1.5: per-device README Connections diagram (Mermaid)** (operator ask). Added the element + a canonical copy-paste template (flowchart LR · up/down subgraphs · host `:::me` · ≤~8 load-bearing nodes) to `Atlas-Documentation-Standard` v1.5 / `Atlas-Documentation-Workflow` v1.5; annotated `ADR-Index` (ADR-0037 row) → v1.15. **Backfilled into all 8 existing device READMEs** (DC · MON01 · NPS01 · PAW01 · FS01 · WSUS01 · SQL01 · RCA01/ICA01). Visual of the connections map; must match the prose (`POL-0008`). Lint-checked (no parens-in-labels, balanced brackets). |
| 0.28 | 2026-07-29 | **PVE02 ACQUIRED (A4 ✅) + compute-topology re-modeled.** Operator bought a **Beelink EQR6** + has an **8 TB external**. `ADR-0036` → **v1.1** (hardware recorded) + **v1.2** (placement **inverted** by uptime — EQR6 = always-on critical tier [DC01·ICA01·NPS01·SRV01·Vaultwarden·FS01·BKP01]; R410 = mostly-off spin-up heavy tier [DC02 cold-standby·MON01·NetBox·cluster node·labs]; 8 TB → FS01+BKP01; backup independence = **mandatory off-site copy**; **64 GB = prerequisite**). `Pre-Build-Decisions` **A4 ✅ acquired / A1 revised** (FS01→PVE02) → **v0.9**. `ADR-0046` → **v1.1** (cluster gate lifted but **on-demand**, + 1GbE-S2D caveat). Two operator decisions taken this session via AskUserQuestion: backup = off-site-copy model; R410 = mostly-off. |
| 0.27 | 2026-07-29 | **`ADR-0048` — Automation & IaC model written** (operator ask: every device page-set carries automation scripts + how-tos; IaC to stand up an Azure env / build a device from a config file). Two-layer model: per-device **`Automation/` doc-type** (Standard v1.4 / Workflow v1.4) + **estate capability** (self-hosted git/CI + shared Ansible/Terraform/DSC/Oxidized — Backlog #7/#19, Phase 10). Learning-Rule reconciled (automate what you've learned by hand); full-stack **phased + cert-matched** (operator); test-gated on idempotency. Propagation: `ADR-0048` → `ADR-Index` v1.14 (ADR-0037/0043 annotated) → owner docs (`Atlas-Documentation-Standard` v1.4 · `Atlas-Documentation-Workflow` v1.4 · `Atlas-Improvement-Backlog` #7/#19 · `Operations/Build-Order-and-Dependencies` Phase 10) → this register → handoff. Every device in the replication wave now inherits an `Automation/` from the start. |
| 0.26 | 2026-07-29 | **`ADR-0047` — FortiGuard-UTM decision written (the one live ADR-set contradiction closed).** Operator is buying the FortiGuard UTM subscription → `ADR-0047` **reverses `ADR-0035`** (FGT01 now runs UTM as the licensed N-S content-inspection layer) and **reshapes `ADR-0038`** (pfSense kept as the *free/complementary* inline IPS + the *free-vs-licensed* comparison, no longer sole N-S prevention). Confidence-trap rule survives as a verify-the-DBs-update duty; subscription-lapse reverts to the `ADR-0035` detach-don't-run-stale fallback. **5-step propagation done:** ADR-0047 authored → `ADR-Index` v1.13 (row added; 0035 superseded / 0038 reshaped annotations) → owner docs (`ADR-0035` v1.1 reversal banner · `ADR-0038` v1.1 reshape · `Atlas-Firewall-Architecture` v1.2 §3.4/§5 · FCP lab-map v1.1 §3 unlocked) → this register → SESSION-HANDOFF. FGT **Build-Guide-3** (Security-Profiles) re-enters the build set (gated on the ICA01 CA cert + a verified live sub). |
| 0.25 | 2026-07-29 | **E2 DONE — estate build-order consolidated** (`ADR-0043`): `Operations/Build-Order-and-Dependencies.md` is the single owner; the two order docs → superseded pointers; tracker → execution log; Service-Architecture → design banner. **`ADR-0044`** — the lab is built to the **real-world enterprise model** as the standard; **certifications anchor the skills** (cert scoping fed by exam-guide TOCs → mapped to Atlas services/roles; gated stubs are *designed*, not placeholders). |
| 0.24 | 2026-07-29 | **Documentation Standard + Workflow → v1.3** (implements `ADR-0043` step 1) — the **phased, gated Build-Guide doc-type**: phases mirror the `Roadmap` 1:1, per-phase 🔴 GATE, standard Certificate-application / Service-setup / Automation-onboarding sections, future/cloud phases as gated stubs, `Build-Guide/` nesting for spine + phase sub-guides. Next: estate build-order consolidation (E2) → DC exemplar reshape. |
| 0.23 | 2026-07-29 | **`ADR-0043` — scalable, phased, dependency-gated Build-Guides + estate build-order consolidation.** Build-Guides become the complete per-device executable (phases mirror the `Roadmap` 1:1; per-phase 🔴 GATE; standard **Certificate-application / Service-setup / Automation-onboarding** sections; future/cloud phases as **gated stubs**). **E2 decided:** consolidate `Master-Build-Order` + `Master-Implementation-Checklist` → one `Operations/Build-Order-and-Dependencies`; tracker → execution log; `Atlas-Service-Architecture` → design + links. Standard Build-Guide doc-type → v1.3 (pending). |
| 0.22 | 2026-07-29 | **Documentation Standard v1.2 (ADR-0037 v1.2).** Formalized four per-device **analytical elements** — 📸 capture markers, `Roadmap` **Certification alignment**, **staged traffic-flow** diagram (visualizes the flows matrix, per `ADR-0041`), and a **Validation link** to `Operations/Validation-and-Adversarial-Testing`. Extended the fact-ownership map: **`Operations/`** owns estate-level operational artifacts (validation, build-order/dependencies, backup/DR, confirmation); device folders hold linking slices. Backlog: **OWASP + SCAP** logged as future foundations. |
| 0.21 | 2026-07-29 | **ADR-0041 logged (D9).** Named the estate's **incremental, test-gated** build discipline (one unit at a time; per-unit positive+negative acceptance gate; ✅ only on a passing gate; adversarial gates → Validation matrix). Global ADR; complements `POL-0006`. Also this session: **audit consolidation** (stale-frontmatter true-ups → Register v0.20 / SRV01 checklist v1.3; DC Roadmap H1 → `ADR-0040` PHS; DC 🔴 tier-account reconcile flag) + **RCA01 Diagnostics** split from ICA01. |
| 0.20 | 2026-07-29 | **Doc-model amendment propagated (E2).** Closed the v0.19 pending-propagation: **ADR-0037 amended → v1.1** (Decision folder-shape + lifecycle now include `Roadmap.md`; new connections-map requirement; amendment note + Consequences bullet) and **ADR-Index → v1.4** (row annotated). Five-step propagation (ADR → Index → owner docs `Standard`/`Workflow` v1.1 → Register → Handoff) now complete. |
| 0.19 | 2026-07-29 | **Restructure exemplar + doc-model codified (E2).** Operator chose *one device, full page-set* → **DC-Domain-Controllers**: added `README.md` (front-door + **connections map**), `Roadmap.md` (per-role build path, Needs/Unblocks), `Considerations.md` — layered over the existing verified DC docs (nothing device-verified was touched). Codified the new **`Roadmap.md`** doc-type + the **connections-map** requirement into `Atlas-Documentation-Standard` (v1.1) + `Atlas-Documentation-Workflow` (v1.1). **Pending propagation:** ADR-0037 amendment (+ ADR-Index) for the doc-set change. |
| 0.18 | 2026-07-29 | **pfSense IDS/IPS future role + Academy capture.** Logged **pfSense** in the estate single-source (`Service-Server-Build-Plan` deferred note) as the operator's planned **inline IPS** — closes the *prevention* gap the passive Suricata-on-SPAN IDS (`ADR-0035`) leaves open; flagged that it needs a **topology ADR** (placement vs 1941/MKT01/FGT01) + coherence with MON01-Suricata/SIEM01-Wazuh when committed. Separately: the operator's `BATlogin.txt` logon script preserved as an Academy concept module (`Atlas-Academy/Concepts/Windows-Logon-Scripts-and-Drive-Mapping.md`, anchored to FS01). |
| 0.17 | 2026-07-29 | **E1 Wave-B execution.** Model = **move-alongside-devices**. Committed roles FS01/WSUS01/SQL01/RDS01/SIEM01-Wazuh added to the estate single-source (`Service-Server-Build-Plan` — new authority **Rule** header per Lab-01 `006`) + given `Devices/` stub homes + README front-doors; `Device-Role-Assignments` reconciled. Deferred: TrueNAS (PBS01=BKP01). **Logged E2**: consolidate the 3 build-order docs → one (Lab-01 model) + backfill the 14 device READMEs. |
| 0.16 | 2026-07-28 | **Planning-doc sprawl logged as E1 (new Section E).** Operator raised that the overlapping planning/status docs (Master-Build-Order, Master-Implementation-Checklist, Build-Progress-Tracker, Service-Server-Build-Plan, Service-Architecture, Device-Role-Assignments, wider Architecture/) can contradict and are hard to follow; some may belong alongside the devices. Proposed: the Wave-B estate roadmap becomes the single source; others link to it per the `ADR-0037` fact-ownership map. Consolidation approach = operator decision at Wave B. |
| 0.15 | 2026-07-28 | **Documentation standard adopted (`ADR-0037`).** Authored `00-Atlas-Foundation/Atlas-Documentation-Standard.md` (v1.0) - the estate per-device/service doc architecture generalized from Lab-01: fixed folder shape, author-in-order lifecycle (checklist-first), `Roles/` per service, a written fact-ownership map (`POL-0008` enforceable), standard per-doc elements + markers. Recorded as **`ADR-0037`** (Scope Global) + `ADR-Index` v1.3; added **D8** to Decisions. Per-wave rollout, not bulk; Lab-01 frozen. Next: capture-workflow companion (how/when each doc is touched at build; Academy fed show+config commands). |
| 0.14 | 2026-07-28 | **Planning session — decisions closed + planning package.** Operator decisions: **`ADR-0035`** (FGT01 no-UTM — Suricata SPAN is the IDS instead), **`ADR-0036`** (compute topology: acquire **PVE02**; DC02+BKP01/Vaultwarden+PAW01 → PVE02; home-PC Hyper-V for non-critical/AZ-802/AD-FS; standalone, no cluster), **Vaultwarden → BKP01** (closes **A3a**), **gap-analysis Tier-A committed** (gMSA/delegation, OCSP+KRA, GPO depth). Produced the planning docs: **`Master-Implementation-Checklist`** (the decision-free bench list), **`Service-Server-Build-Plan`**, **`Operations/SoT-Evidence-Run-Sheet`**, **`NETBOX01/NetBox-Data-Load-Prep`**, **`00-Atlas-Foundation/Public-Release-Manifest`**. Still-open (non-blocking, flagged): Vaultwarden master-pw recovery (`049`), PVE01 board (`CM-0012`), GPO Wave-B VBS check. |
| 0.13 | 2026-07-28 | **B1 CLOSED — repo front-door + glossary (the last learnability item).** Rewrote the stale root **`README.md`** into the single repo front door: a *start-here-by-what-you-need* table (highlights→PORTFOLIO, where-we-are→handoff, rules→Foundation, plan→Roadmap/Master-Build-Order, why→Concepts, how-to-verify→Command-Library, terms→Glossary), a current repo map, the conventions (`POL-0001`/`POL-0008`/markers/IDs), and an explicit **front-door hierarchy**. Created **`GLOSSARY.md`** (Atlas process, identity/Windows, networking, PKI/security, virtualization/time, device names). Reconciled the 3 competing front doors with role banners pointing back to the README (Blueprint = historical snapshot; PORTFOLIO = reviewer entry; Foundation README = Foundation index). **The entire B-series (B1–B5) is now closed.** |
| 0.12 | 2026-07-28 | **D6 ACCEPTED (operator) + Academy `Command-Library/` built.** D6 (adopt Atlas Academy) ratified by the operator — Academy README/Concepts flipped from "needs accept" to accepted. New **`Atlas-Academy/Command-Library/`** (`ADR-0032`): **platform-first deep pages** — `PowerShell-Tier0.md`, `Cisco-IOS.md`, `RouterOS.md`, `FortiOS.md` (full) + `Linux.md` (expanding: PVE01 grounded, Pi01/SRV01/NetBox/MON01 seeded) + `README.md` index cross-indexed by service and failure-category. Each command written as purpose → command → healthy → broken → grounds; per-platform read-back discipline carried (`show`-status/`print detail`/`get`-not-`show`/`Test-NetConnection`). The per-device `Diagnostics.md` quick-refs now link up into it. 🟡 commands are lab-unverified until a read-back is pasted (`POL-0001`). Remaining: expand Linux service hosts + DHCP/service pages as those build. |
| 0.11 | 2026-07-28 | **B-series learnability + Diagnostics seeding (docs-only).** **D6 = adopt Academy** (operator-chosen; 🔴 needs explicit accept): Academy README → Adopted; new **`Concepts/`** layer — **B2** `Tiered-Admin-Model.md` one-pager + 303 Part-5 cross-link; **B3** `Concepts/README.md` seeds all 8 uncovered-concept targets (FSMO/DFSR/SCT/VBS·CG/DSRM + OSPF/DAI/RouterOS-v7); **B4** `Atlas-Teaching-Patterns-and-House-Style.md`. **B5** tracker v0.20 `Current focus` restructured (handoff = single owner). **`ADR-0032` Diagnostics seeded for all 8 built devices** (DC01, DC02, ICA01, MKT01, SW01, 1941, FGT01, PVE01) from the template — 🟡 lab-unverified, ✅ where a prior read-back exists. Remaining: B1 front-door/glossary, D5 lab, DC02 read-back, Confluence redirect. |
| 0.10 | 2026-07-28 | **C7 fully closed + `ADR-0034`.** Operator decided PVE01-networking ownership: authoritative home = the new **`Labs/Lab-02-Cisco-Core/Virtualization/Build-Records/PVE01-Networking.md`** (Virtualization book), consolidating the current device-verified design (`204` v1.2 — tagged `vmbr0.10` `/27`, native 999, `bridge-vids`); the two **frozen Lab-01** homes + `204` become pointers/procedure; Confluence flagged for manual redirect. **`ADR-0034`** authored (Scope Global — active-book-owns-live-config principle) + added to `ADR-Index`. Manifest → v1.2 (Freeze #2 resolved; Build-Records 2→3). C7 🟡→✅; residual storage/auth records + Validation stub are non-blocking backlog. |
| 0.9 | 2026-07-28 | **Phase-2 loose ends closed (docs-only, no lab).** **A2a ✅** IP-plan v1.4 — NPS01 row `10.20.0.12` (server range). **A2b ✅** Allowed-Flows-Matrix v1.2 — flow #14 network-device RADIUS→NPS01 (UDP 1812/1813), NPS kept out of the `.2–.9` micro-zone, flow #9 annotated. **A4a ✅** AD-CS guide v0.3 — §3.5 build-time NPS server cert (`ADR-0029`) + **Part 3B** non-domain enrollment & RCA01-trust distribution (Pi-hole/MKT01/FGT01, `ADR-0031`). **C4 ✅** CIS-1941 v1.1 / CIS-SW01 v1.1 / CIS-MKT01 v1.2 (+ MKT01 Build-Guide v0.8) — Pass-2 auth = RADIUS→NPS on `NPS01`, MKT01 "AD-LDAPS admin" corrected. **C5 ✅** CIS-FGT01 v1.2 + FGT Build-Checklist v1.1 (admin auth → `ADR-0028`/Guide-2b, RADSEC struck) + FGT Index v0.3 (Troubleshooting + Logging added). **C6 ✅** MKT01 firewall-doc disambiguation note + Troubleshooting v1.1 stale-version fix. **C7 🟡** Virtualization manifest v1.1 (numbering-collision resolved, DC01-promoted updated) — PVE01-networking ownership decision + thin records still open. 🔴 **Also found + fixed the A1 gap:** the IP-plan DHCP wording was still "Kea on SRV01" at HEAD though v0.8 recorded it flipped — corrected to DC01 (IP-plan v1.4). All edits pending Seth commit. |
| 0.8 | 2026-07-28 | **Phase 1 finished + Phase-2a cascade complete + ADR scoping.** All committed. **ADRs:** `ADR-0030` (DHCP→DC01) + `ADR-0031` (retire OpenSSL) committed (`b24b1be`); **`ADR-0032`** (diagnostics/verification doc architecture — per-device `Diagnostics.md` + Academy command library + continuous-handoff protocol) + Diagnostics template committed; **`ADR-0033`** (every ADR carries a **Scope** — Global/Lab-01/Lab-02, "Global if the principle is estate-wide") + **`ADR-Index.md`** committed, all 32 prior ADRs backfilled with Scope. **Reconciliation:** `Master-Build-Order`→v1.6 (DHCP→DC01 Phase-3h, NPS→NPS01, Phase-8 unified AD CS) and the full downstream cascade — Device-Role-Assignments v1.1, VM-Inventory, Atlas-Service-Architecture v1.2 (preamble), SRV01 Guide v0.2 + Checklist v1.2, CA01-VAULT01 v1.1 (superseded), Build-Progress-Tracker v0.19; plus M10/DC02 across IP-plan/303/304/DC-checklist (DC02 = 🟡 operator-reported, read-back pending). **Closed:** D1 ✅, D4 ✅, A1 ✅ (Phase-2a done), A3 ✅ (Phase-2a done, A4a open), A2c ✅, **C1 ✅, C2 ✅, C3 ✅**, A3a 🟢 approach-settled (web-console standalone), B1 🟡 partial (ADR index). **Still OPEN → Phase 2:** A2a (NPS01 IP-plan row), A2b (east-west RADIUS flow), A4a (AD-CS guide NPS cert), C4/C5/C6/C7, B2–B5, per-device `Diagnostics.md` seeding for built devices, D5 migrate-and-test lab. |
| 0.7 | 2026-07-28 | **Phase 1 — D1 + D4 closed (2 of 3 remaining ADR actions done; ADR-0029 amendment was the 1st).** Drafted **`ADR-0030` — DHCP on DC01** (captures **A1**): fewer-VMs driver, failover DC01-now/DC02-later, dedicated DDNS service account + **no `DnsUpdateProxy`**, and a head-on reconciliation with the estate's role-separation posture (`ADR-0027` CA-off-DC, `ADR-0029`/D7 NPS-off-DC) — DHCP holds no Tier-0 credential and nothing in Tier-0 depends on it. Drafted **`ADR-0031` — retire OpenSSL Lab CA** (captures **A3**): reverses `ADR-0003`, folds Pi-hole/MKT01/FGT01 onto AD CS with `ADR-0009` reissue cost stated, **decommissions CA01-VAULT01 entirely**, migrate-and-test (D5) before retiring. Marked **D1 ✅ / D4 ✅** and **A1 ✅ / A3 ✅** (captured; doc-row execution → Phase 2). **Operator changed C3's assumption:** host decommissioned, not renamed → added new ripple **A3a (Vaultwarden relocation — host TBD)**. Both ADRs drafted this session, **pending Seth commit**. |
| 0.6 | 2026-07-28 | **Phase 1 — D7 closed.** `ADR-0029` amended to **v1.1**: NPS host = dedicated member server **`NPS01`** (not the DC), MS NPS-on-DC perf guidance logged as the rejected alternative, PEAP cert deferred→build-time, availability caveat + break-glass retained. Created **`Devices/NPS01-Network-Policy-Server/Build-Guide.md`** (v0.1 placeholder). Marked **D7 ✅ DONE**, **A2 amendment ✅** (estate → Phase 2), **A4 recorded** (AD-CS-guide edit → Phase 2). Added **Section A2-ripples** (A2a IP-plan row, A2b east-west RADIUS flow, A2c service-arch/role rows, A4a AD-CS-guide cert line) so the D7 follow-on is tracked, not re-audited. |
| 0.5 | 2026-07-27 | **Re-baselined + refocused** after `git pull` (clone was 4 commits behind). Discovered the 07-24 doc-conflict audits + `ADR-0029` (FreeRADIUS→NPS, on the DC). D2 → DONE (0029); D7 → amend 0029 (member server); D1/D4 confirmed as the new forward decisions. Restructured to Sections A (new-decision ripples), B (learnability), C (device/ops — unscoped by 07-24), C′ (D5), D (deferred-to-07-24 map). Added the current-design baseline (native-999) + delta-file caveat. |
| 0.1–0.4 | 2026-07-27 | Original F1–F48 (pre-re-baseline; superseded by this refocus — retained in git history). |
