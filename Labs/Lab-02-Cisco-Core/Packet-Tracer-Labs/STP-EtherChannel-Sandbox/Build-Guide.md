---
Title: STP + EtherChannel Sandbox — Build-Guide
Path: Labs/Lab-02-Cisco-Core/Packet-Tracer-Labs/STP-EtherChannel-Sandbox/Build-Guide.md
Status: 📋 SCAFFOLD — build procedure (fill per stage)
Version: 0.1
Date: 2026-08-05
---

<!-- provenance -->
> **Lab-02 · Cisco-Core · Packet-Tracer-Labs (🖥️ simulator, `ADR-0022`).** This documents the **STP + EtherChannel Sandbox** built in Packet Tracer. A ✅ here is **🖥️ sim-verified**, distinct from a hardware ✅. Design lives in the spec (linked below); this page-set is where the *actual build* is recorded.

> 📋 **SCAFFOLD — fill this in.** Section headers + prompts are pre-placed to match the Atlas device page-set; the content is intentionally blank so the operator documents the real build and tests the layout.

# STP + EtherChannel Sandbox — Build-Guide (how to build it in PT)

> _Target state: the exact devices, cabling, and config to reproduce this lab from scratch. Type the config; capture the verify in the Build-Record._

## 0. Prerequisites

> _PT version, the device models to drop, the addressing plan reference._

## 1. Physical — drop + cable the devices

> _Fill: node list + the port map (which interface to which)._ 

## 2. Base config (hostnames, VLANs, trunks, addressing)

> _Fill: the config to type, stage by stage._

```
<!-- config -->
```

## 3. Feature stages

> _One sub-section per objective (e.g. OSPF/DR-BDR, HSRP, EtherChannel, STP, ACLs, DHCP relay, wireless). Config + a Verify line each._

### 3.1 <feature>
```
<!-- config -->
```
**Verify:** `<!-- show command -->` → _expected read-back._

## 4. Revert / teardown

> _How to reset or the safe end-state._

## Lessons Learned from Actual Deployment

> _Fill AFTER building — the gotchas the sim actually threw._

## Change Log
| Version | Date | Change |
|---|---|---|
| 0.1 | 2026-08-05 | Scaffold created (blank page-set for the operator to fill). |
